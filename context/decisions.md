# decisions - decisions made and rejected

Owns: dated decisions, including options considered and turned down, with reasoning. Does NOT hold open questions (context/open_questions.md) or tasks (context/todo.md).
Maintain: append-only; never rewrite a past entry.
Entry format:
## YYYY-MM-DD - <decision>
Why: <reasoning>
Rejected: <option turned down, if any>

## 2026-07-14 - Deployment procedure captured from the internal deployment runbook
Why: the internal deployment runbook is the source of truth for the Protectli/Ubuntu Domotz provisioning runbook; captured here as docs/deployment-guide.md so it's versioned and can drift-check against the live internal runbook.
Rejected: n/a

## 2026-07-14 - New sub-project: dynavlan (dynamic VLAN auto-configuration)
Why: Static netplan with hardcoded VLANs requires internal users to edit YAML or SSH in to run a wizard, which is untrusted and does not scale as deployments move off a single standardized baseline. dynavlan is a self-configuring, company-agnostic tool that discovers active VLANs at boot and every 5 min and brings them up with DHCP, zero SSH, zero YAML editing in the common case.
Rejected: (a) Ubuntu/netplan built-in dynamic VLAN detection - does not exist, VLANs must be statically declared. (b) Domotz "assisted configuration" scripts (network_config.sh, add_vlans.sh) - interactive wizards only, no auto-detection, hardcode a single netplan path and rewrite it destructively, no DHCP option for VLANs. (c) `apt install vlan` + vconfig - legacy, netplan needs none of it. (d) Pentest VLAN-discovery tools (PyScanVLAN, 802.1q_network_scanner) - not built for unattended production DHCP provisioning.

## 2026-07-14 - dynavlan design locked (naming, detection, config schema, behavior)
Why: Full design agreed in planning session. Naming: project `dynavlan` (GitHub-clean: 0 exact repo matches, vs autovlan/vplan crowded, dynaplan squatted); generated netplan file `90-dynavlan.yaml`; config `/etc/dynavlan.conf`; script `/usr/local/sbin/dynavlan`; units `dynavlan.service` + `dynavlan.timer`.
Design invariants locked:
- Base netplan config is never read for assumptions nor modified; dynavlan owns ONLY 90-dynavlan.yaml. Makes it company/hardware-agnostic.
- Physical interfaces discovered dynamically (/sys/class/net, device symlink + type==1, exclude lo/wireless/VLAN), never hardcoded names. Trunk discovered by evidence: sniff every physical port for tagged frames; port carrying tags is the trunk; if multiple, port with most distinct tags wins, others logged/ignored.
- Detection = union of passive 802.1Q sniff (switch-agnostic) and lldpctl VLAN TLV (deterministic when advertised), controlled by DETECT_METHOD (both|lldp|sniff, default both).
- Candidates = detected ∩ [VLAN_MIN,VLAN_MAX] − (VLANs defined in any OTHER netplan file, matched by ID not interface) − (already in our file). External/base netplan VLANs are authoritative; excluded by ID to avoid key collisions and duplicate-subnet. Source of "defined elsewhere": `netplan get network.vlans` minus our own file. Warn on overlap between our file and an external file.
- Generated VLANs: dhcp4 true, use-default-routes false (untagged uplink from base config is the only default route).
- Safety: validate with `netplan generate` + backup before every apply; roll back on failure (headless box, no SSH recovery).
- Change-gated side effects: only a genuinely new/changed VLAN set triggers apply + restarts. Order: apply -> DHCP leases settle -> restart RESTART_SNAPS -> restart RESTART_SERVICES. Missing/failing entry logged, does not abort the rest. Steady state = zero applies, zero restarts.
- Persistence: generated 90-dynavlan.yaml IS the known-set (no separate state file). Add-only on the 5-min rescan (never remove, immune to idle-VLAN sniff misses). Boot = full reconcile to freshly-detected set (adds AND removes stale/relocated VLANs) done in a single reconcile pass (no destructive wipe-then-reapply), so an unchanged site reboot causes zero churn. RESET_ON_BOOT default true; false = add-only across boots, removal only by reimage.
- Config file `/etc/dynavlan.conf` shipped with all keys commented (built-in defaults apply untouched): DETECT_METHOD=both, VLAN_MIN=1, VLAN_MAX=1000 (valid 802.1Q range is 1-4094; 0/4095 reserved), SNIFF_SECONDS=60, RESCAN_MINUTES=5, RESET_ON_BOOT=true, RESTART_SNAPS="domotzpro-agent-publicstore", RESTART_SERVICES="". Invalid values are rejected (script refuses to run) rather than silently defaulted.
Rejected: hardcoded enp1s0 trunk (fails on other Protectli NIC naming); reconcile (drop-unseen) on rescan (tears down idle VLANs Domotz monitors); naming the netplan file after Domotz (tool is now vendor-agnostic); RESET_ON_BOOT=false as default (leaves ghost VLANs after relocation, no self-heal without SSH).

## 2026-07-14 - dynavlan logging and operational hardening locked
Why: Box is headless with no expected SSH access, so logging is the only forensic window and several operational gaps need closing before build.
- Logging: sink is systemd journal via stderr, syslog identifier `dynavlan` (journalctl -u / -t). Optional rotated /var/log/dynavlan.log. LOG_LEVEL config knob (debug|info|notice|warning|err, default info). Every run logs a start and end line so silent failure shows as a missing end line. State-change events at notice: VLAN discovered (ID/interface/method), VLAN removed on boot reconcile (ID+reason), netplan applied, each snap/service restarted, boot reconcile summary, reset event. info: run start (boot vs rescan, effective config), interfaces discovered, trunk selected, scan start/end, candidates vs known, no-change, leases acquired. warning: VLAN excluded (defined in other netplan file + which), our/external overlap, restart entry missing/failed, VLAN got no lease in settle window, VLAN count over sane threshold. err: invalid config (refuse run), no trunk, netplan validate fail, apply fail+rollback, tcpdump/lldpctl missing.
- Backup retention: BACKUP_KEEP config (default 10); prune oldest beyond it. Backups double as change-history audit trail. Fixes unbounded .bakN growth from Domotz's scripts.
- Concurrency: each invocation wrapped in flock; overlapping run logs "skipped, run in progress" and exits. Prevents colliding netplan applies from timer stacking on a slow run.
- One script, two modes: `dynavlan --boot` (full reconcile) from dynavlan.service, `dynavlan --rescan` (add-only) from dynavlan.timer.
- Boot ordering: service orders After=systemd-networkd.service, does NOT hard-block on network-online.target (sniff needs link-up not an IP, and script brings links up itself); lease-settle wait precedes snap/service restarts since Domotz needs networking up.
- Dependency: `tcpdump` added to deployment package list (sniff needs it; not currently installed).
- Scale guard: warn-and-proceed threshold when about to create an unusually large number of VLANs (misconfigured wide VLAN_MAX on a many-VLAN trunk).
- Privacy: logs contain client VLAN IDs and subnet addresses (client network topology under confidentiality obligations). Logs stay local; nothing ships them off-box without a deliberate decision.
- Conventions: VLAN interfaces named `<iface>.<id>` (e.g. enp1s0.20); networkd renderer inherited from base, never re-declared.
Config additions: LOG_LEVEL=info, BACKUP_KEEP=10 (both commented defaults in /etc/dynavlan.conf).
Rejected: unbounded backups; journald-only with no file option (kept file optional); blocking on network-online.target (would stall a no-uplink boot).

## 2026-07-14 - dynavlan is filename-agnostic; discovery vs exclusion sources locked
Why: Base netplan filenames vary and cannot be relied on (verified on the lab box: enp1s0 defined across BOTH 00-installer-config.yaml and 01-netcfg.yaml). dynavlan must hardcode exactly ONE path: its own /etc/netplan/90-dynavlan.yaml. Everything else is discovered, never read by base filename.
- Physical interface + trunk discovery: LIVE Linux state only (/sys/class/net + ip link for interfaces; sniff for trunk). Never reads netplan. Works even if base config is missing or names nothing.
- VLAN exclusion set (VLANs to skip because managed elsewhere) = union of two filename-agnostic sources, minus dynavlan's own IDs:
  (a) PRIMARY: `netplan get network.vlans` - merges ALL /etc/netplan/*.yaml regardless of name; this is the netplan layer's own answer and the collision we avoid (two files defining same vlan) is a netplan-layer problem. Verified working on live box across the split base files.
  (b) SAFETY NET: `ip -d link show type vlan` (extract `802.1Q id N`) - catches VLANs created OUTSIDE netplan (raw ip link, other tools, other renderer .netdev) that netplan get would miss (architect M3).
- Exclusion matched by numeric VLAN ID, not interface name. candidates = detected ∩ [VLAN_MIN,VLAN_MAX] − exclusion − our-own-IDs.
- Consequence: a VLAN declared in netplan but not yet instantiated is still excluded (via netplan get); a VLAN instantiated outside netplan is still excluded (via ip link). Belt-and-suspenders that makes "leave everything else alone" actually hold.
Verified 2026-07-14: on the lab box both sources returned identical {1,21,22,101}; live sniff additionally discovered unconfigured VLAN 18 on the trunk.
Rejected: reading/globbing base YAML files by name (fragile); using only netplan get (misses non-netplan VLANs); using only live ip link (misses not-yet-applied netplan VLANs, and can't tell configured from ad-hoc).

## 2026-07-14 - dynavlan netplan behaviors validated on live box (C1 + VLAN stanza + removal)
Why: End-to-end test on the lab box (Domotz agent unlinked) exercised dynavlan's exact apply path and surfaced two non-obvious netplan/networkd behaviors that change the design.
1. APPLY PATH VALIDATED: write /etc/netplan/90-dynavlan.yaml (mode 600, root:root) -> `netplan generate` (validates, exit 0) -> `netplan try --timeout N` fed by a health-gated stdin -> health check (default route still via base uplink) -> printf newline to ACCEPT, else let it revert. Added VLAN 18 (enp1s0.18) which came up with a DHCP lease and NO default route. Confirmed headless/no-TTY.
2. CORRECT ISOLATION KEYS: the netplan key is `use-routes: false` (NOT `use-default-routes`, which is not a netplan key). But use-routes:false alone still leaves networkd installing link-scope host routes for DHCP-provided DNS/NTP servers - observed public OpenDNS IPs (208.67.220.220/.222) and stray /32s pinned to the VLAN interface. Under confidentiality obligations that means the monitor could resolve via a site VLAN's DNS or pin public IPs to the wrong interface. FULL isolation stanza (validated: interface then has ONLY its connected subnet route):
   dhcp4: true
   dhcp4-overrides: { use-routes: false, use-dns: false, use-ntp: false, use-domains: false }
   dynavlan VLAN stanzas MUST include all four.
3. VLAN REMOVAL REQUIRES EXPLICIT `ip link delete`: removing a VLAN's stanza from netplan and running `netplan apply` does NOT remove the live interface - networkd leaves the orphaned VLAN device UP (still had its lease) until reboot. Confirmed: after rm + netplan apply, enp1s0.18 was still up; only `ip link delete enp1s0.18` removed it. CONSEQUENCE: dynavlan's boot reconcile (FR-18) and relocation self-heal (AC-3) must, for each VLAN being removed, both drop it from 90-dynavlan.yaml AND explicitly `ip link delete <iface>.<id>`. netplan apply alone is insufficient for removal.
Also noted: netplan warns "Permissions too open" on world-readable base files (00-installer is 644); dynavlan's own file is 600 so it won't trigger this. `netplan try` accept/revert both work over SSH with no TTY (drive accept via fifo/coproc at build time).
Rejected: use-routes:false as the sole isolation key (leaks DNS/NTP host routes); relying on netplan apply to remove VLAN interfaces (leaves orphans).

## 2026-07-14 - dynavlan VLAN_IGNORE denylist added (no allowlist)
Why: Original "take all, disable unwanted at the switch port" assumes we control the trunk. Post-standardization, on site-managed switches we often won't, so an in-config denylist is needed to keep the monitor off specific VLANs (guest/voice/contractually-off-limits segments) even when they're trunked and detected.
- New config key: `VLAN_IGNORE=""` (commented default empty = take-all preserved). FORMAT: list-with-ranges, separators are comma and/or whitespace, ranges via `low-high` inclusive. Example `"1,5,20-25,80,30"` -> set {1,5,20,21,22,23,24,25,30,80}. Parsed as a set (order-independent, duplicates/overlaps collapse). Validation: each ID 1-4094, range low<=high, reject non-numeric/malformed -> refuse to run + log err. Ignoring an ID outside [VLAN_MIN,VLAN_MAX] is a harmless no-op. VLAN_MIN/VLAN_MAX stay single integers (no list syntax).
- Precedence: ignore ALWAYS wins. candidates = detected ∩ [VLAN_MIN,VLAN_MAX] − VLAN_IGNORE − (managed in other netplan files) − (our own IDs).
- Composes with VLAN_MIN=2 default (range handles VLAN 1; VLAN_IGNORE handles specific IDs).
Rejected: allowlist / VLAN_ALLOW whitelist mode - defeats the auto-discovery purpose (back to declaring VLANs by hand); user chose denylist only.

## 2026-07-14 - dynavlan PRD v3: apply/rollback sequencing + two approved decisions
Why: Second architect review of PRD v2 found the design sound but the apply/rollback/removal SEQUENCING under-specified (4 CRITICAL paper-closable gaps). PRD v3 (docs/dynavlan-PRD.md) closes them. Note: VLAN_MIN default is 2 (PRD authoritative); the earlier "design locked" entry line saying VLAN_MIN=1 is SUPERSEDED.
Sequencing decisions locked in v3:
- FR-18 health check: SNAPSHOT the pre-apply default route (iface+gw+metric) as the reference (dynavlan never reads base config, so this is its only notion of "the base uplink"); revert gate = default route still egresses the snapshot interface; gateway reachability is secondary/non-fatal and L2/ARP not ICMP (HNW sites filter ICMP -> false reverts); invariant netplan-try-timeout > max-health-check + margin; VLAN lease failures NEVER revert.
- FR-30 flock: held on an OPEN FD (kernel-released on death), never a manual lockfile - else a mid-try death permanently freezes the tool in "skipped, run in progress".
- FR-24 removal ordering: explicit `ip link delete` ONLY after netplan try ACCEPT, never before, never on revert - else the validated orphan behavior silently loses monitoring when a change reverts.
- FR-0 startup: capability-probe netplan try + pinned min netplan version; refuse to run if unavailable; NO silent fallback to bare netplan apply (would drop the revert safety net).
- FR-22/23: boot ADD is single-pass; boot REMOVE requires two passes within the same --boot run (sniff, BOOT_SETTLE_SECONDS, sniff); rescan stays strictly add-only.
- FR-27: a reverted apply does no lease-settle and no restarts.
APPROVED DECISIONS: (1) Minimum netplan version pinned to Ubuntu 22.04 baseline (netplan 0.106), the validated version. (2) PER_VLAN_MAC default OFF (shared parent MAC validated working on lab box; derivation has bit-flip/collision footguns) - kept as opt-in insurance only.
Also in v3: FR-10 re-tagged HIGH/CONTINUITY (dup key caught by netplan generate, doesn't strand); refuse-to-run if sniff requested but tcpdump absent; mandatory install-verified log persistence; minimal-snaplen sniff w/ no on-disk capture (confidentiality-sensitive); promisc left on deliberately; distinct err "our YAML bad" vs "base file bad"; fifo/coproc try-accept promoted to FR w/ SIGPIPE + wall-clock guard; AC-10 second untagged NIC never selected as trunk; AC-11 reverted change leaves no deleted interfaces.
Rejected: silent fallback to netplan apply when netplan try absent; ICMP-based health check; manual lockfile; deleting interfaces before accept; PER_VLAN_MAC default-on.

## 2026-07-14 - dynavlan implementation decisions + portability seam (dev/features/dynavlan.md)
Why: Wrote the technical design doc bridging PRD v3.1 to code. Locked implementation-level decisions.
- Language: bash (zero-dependency, cross-distro at the shell level, natural for netplan/ip/tcpdump/lldpctl orchestration; concurrency footguns contained in the flock/fifo functions).
- Timer interval: static systemd timer + a `.d/interval.conf` drop-in rendered from RESCAN_MINUTES by `dynavlan --reconfigure` (also at install) + daemon-reload; runtime RESCAN_MINUTES edits need a re-run. Avoids self-rescheduling loop / long-running daemon.
- Two-pass boot removal state: in-process within the single --boot run (detect, sleep BOOT_SETTLE_SECONDS, detect; remove only VLANs absent from both). No persistence; rescan stays strictly add-only.
- PORTABILITY SEAM (pre-planned for future Linux flavors, structure-only, not built): core logic is distro-agnostic (interface discovery, sniff/lldp, filter/exclude, health-check eval, logging). All netplan-coupled code isolated behind a 6-operation backend seam: list_managed_vlans, owned_vlans, generate_config, validate, apply_with_revert, remove_vlan. netplan is the ONLY implementation now. Ownership stated abstractly ("own a VLAN-definition namespace, exclude others"). backend_detect stub resolves only to netplan. apply_with_revert is a CAPABILITY contract ("apply with health-gated auto-revert"), not netplan-specific - the one genuinely hard portability item since revert varies: netplan try / NetworkManager native checkpoints (cleaner) / systemd-networkd-direct + ifupdown (hand-rolled, none).
Rejected: building multiple backends now (speculative); plugin loader / backend-selection config (until backend #2 real); non-systemd init support (systemd assumed for service/timer/journald); python (interpreter-version coupling hurts the portability goal).

## 2026-07-15 - Layer 3 testing runs on real hardware on the live Meraki trunk, not a VM
Why: The original test plan pushed Layer 3 (apply/rollback safety) onto a console-accessible VM because the lab box was treated as SSH-only and a failed revert could strand it. User confirmed direct CONSOLE access (keyboard+monitor or serial) to the actual Protectli appliance, which satisfies the one hard requirement (a recovery path independent of the box's own uplink) without a VM. Real hardware is strictly better as a test bed: the live Meraki trunk supplies real tagged frames so sniff/LLDP detection is exercised for real (not approximated by a synthetic/namespace bridge), and the igb-NIC + netplan behaviors under test (promisc bypass of rx-vlan-filter, netplan try/revert, explicit ip link delete) are the exact ones already hardware-validated. netplan try still auto-reverts on timeout, so most cases self-heal with no intervention; the console is the backstop for the residual cases where the safety net itself is under test (L3-6 health-check revert, L3-11 flock death-release, L3-12 kill-mid-generate). Updated: dynavlan-tests.md Layer 3 heading/intro/philosophy + L3-8/L3-10 wording + out-of-scope note; todo.md Step 5; CLAUDE.md doc table + dev-workflow note.
Rejected: VM with console access (a synthetic trunk only approximates detection and adds a NIC/offload layer that differs from the validated igb hardware); building a namespace-based virtual trunk harness (unnecessary while real hardware is available).
Bearing on build: Layer 1 (unit asserts) and Layer 2 (--dry-run) remain the pre-hardware gates; Step 2 is written to be exercised via Layer 2 --dry-run first, then the Layer 3 hardware checklist. Nothing in Step 2 code changes as a result of this decision.

## 2026-07-23 - Step 4 review pass done (code-reviewer + security-reviewer); all findings fixed
Why: Parallel subagent review of the full artifact set vs PRD v3.1 + design doc. Verdict: 0 CRITICAL; apply/rollback chain, health-check gating, delete-after-accept, fd-flock, 0600 perms, and no-exfiltration all confirmed sound. Findings fixed same day (script went 933 -> 1020 lines):
- HIGH (code): do_boot's two passes could sample DIFFERENT physical NICs (run_detection re-selects the trunk each call; hysteresis can be superseded), so boot_removals could compare tag sets from different wires and tear down a live-but-idle owned VLAN. Fix: do_boot now pins to owned_parent for both passes and the generate (mirrors do_rescan); pass 2 counts for removals only if the reference iface has carrier at pass-2 time; run_detection clears every stale tag stash. Boot never relocates a live parent.
- MEDIUM (code) C1: backend_list_managed_vlans included dynavlan's own ids (netplan get merges our file; ip link lists our live subifaces), producing a false "managed elsewhere" warning for every owned VLAN on every run. Fix: subtract owned inside the function (matches its FR-10 contract). While fixing, found warn_overlap (FR-11) was DEAD CODE (subtracted owned from managed then searched owned in the remainder - never fires); reworked to count duplicate id definitions in merged netplan get output (>= 2 keys with the same id = defined in another file too).
- MEDIUM (code) C2: SNIFF_SECONDS=0 passed validation but `timeout 0` disables the bound -> tcpdump hangs forever. Fix: SNIFF_SECONDS >= 1 enforced (other *_SECONDS may stay 0).
- MEDIUM (code) C3: detection cost was sequential per NIC (carrier waits stacked; sniff ran even on dead ports), able to blow TimeoutStartSec on many-port hardware. Fix: up+promisc ALL NICs first (waits overlap), sniff only carrier-up NICs.
- MEDIUM (security) S1: eval-based TAGS_<iface> stash spliced a kernel-supplied iface name into an eval -> latent root code-injection primitive. Fix: discover_phys_ifaces rejects names outside [A-Za-z0-9_.-]; tags_var() sanitizes the var name ([^A-Za-z0-9_] -> _). Rejected: associative array (breaks bash-3.2 sourcing for the local test runner).
- MEDIUM (security) S2: load_config sourced /etc/dynavlan.conf as root with no ownership/perm check -> perm drift = local root escalation. Fix: when euid=0, refuse unless root-owned and not group/other-writable.
- MEDIUM (security) S3: atomic-write temp used a predictable $$-based name. Fix: mktemp in the same dir (atomic same-fs rename preserved).
- LOW: backup dir now mkdir -m 700; do_dryrun checks backend_generate_config before validating (was silently validating a stale file on write failure).
All 35 unit asserts stayed green; config smoke re-run incl. new SNIFF_SECONDS=0 rejection. Design doc §5/§6 and dynavlan.conf updated to match.

## 2026-07-23 - Round-2 review (Fable model): fifo-ACCEPT race + relocation deadlock; all findings fixed
Why: Second code review (fresh reviewer on the Fable model, briefed to verify round-1 fixes and hunt what both prior reviews missed) returned BLOCK: 1 CRITICAL, 4 HIGH, 4 MEDIUM, 3 LOW. All fixed same day (script -> 1242 lines). Round-1 fixes all verified correct except one regression it introduced (relocation).
- CRITICAL (fifo-ACCEPT race): netplan try APPLIES before reading stdin; our accept was written after a fixed 2s health sample, so for any apply slower than 2s the check saw PRE-apply routing (trivially PASS) and the buffered newline was consumed unconditionally post-apply - nullifying FR-18 for the exact case it exists for (valid-but-wrong base file killing the uplink at apply time). Fix: backend_apply_with_revert is now a poll loop gating accept on (a) apply-completion evidence - the first added VLAN's iface exists (probe), fallback settle floor for removal-only changes; (b) netplan-try liveness re-checked at the accept instant; (c) HEALTH_CONSEC consecutive health PASSes; all bounded by an independent wall-clock guard (TRY_TIMEOUT*3). HEALTH_SETTLE constant retired.
- HIGH dead-try false accept: a netplan try that exits early (e.g. "another netplan process running") left routing untouched, health PASSed, and we accepted a never-applied change (then deleted interfaces + restarted the agent for it). Fix: liveness gate above; dead try = never accept.
- HIGH FAIL-path EOF: we closed the fifo write-end ~28s before netplan try's revert timer; its stdin-EOF behavior is NOT hardware-validated (could crash without reverting). Fix: on FAIL hold the write end OPEN until netplan try exits on its own validated timer path (bounded).
- HIGH relocation deadlock (round-1 regression): parent-pinning + pass-2 carrier gate composed to make cross-NIC trunk relocation impossible (owned VLANs stranded on a dead parent forever; AC-3 broken). Fix: explicit relocation branch in do_boot - if the owned parent carries no tags in BOTH passes while a different iface was independently selected with tags in BOTH passes, reconcile in one pass (remove all owned from old parent post-accept, provision new trunk's set on new parent). Single-pass evidence does NOT relocate.
- HIGH detection scaling: per-port carrier waits and sequential sniffs could blow TimeoutStartSec on 4-6-port hardware. Fix: one shared carrier deadline (CARRIER_WAIT_SECONDS total) + concurrent sniffs (one SNIFF_SECONDS window total), sequential fallback if the scratch dir cannot be created.
- MEDIUMs: BACKUP_KEEP >= 1 enforced + backup cp checked (failure refuses the apply) + prune moved post-accept (BACKUP_KEEP=0 could leave restore_prior targetless and wipe the known-set from disk); dry-run mktemp -d checked (unchecked failure would have made dry-run write the REAL netplan file - FR-34 violation); install.sh no longer `--now`s the timer (monotonic OnBootSec already past = immediate rescan apply before the operator's dry-run; timer now armed for next boot); wall-clock guard implemented (was a stated FR-18 invariant, previously missing).
- LOWs: --status requires root (unprivileged it silently probed nothing and reported a confident wrong answer); dry-run mirrors the boot/rescan trunk pinning; FR-11 same-key-merge limitation documented in design §11.
PRD bumped to v3.2 (FR-18 accept-evidence clause, FR-19 backup semantics); design §6/§9/§11 rewritten to match; hardware checklist gained L3-15..L3-17 (accept race, FAIL-path revert, dead-try guard) and L3-18 (cross-NIC relocation).

## 2026-07-23 - VLAN_LIMIT hard cap added (FR-36); VLAN_COUNT_WARN renamed VLAN_WARN
Why: User-requested guard against runaway provisioning (misconfigured wide range, or box patched into a core trunk). New config: VLAN_LIMIT (default 64, 0 = unlimited) caps the total VLAN set (kept + additions); VLAN_LIMIT_MODE (default refuse) picks the over-limit behavior. refuse = apply NOTHING new (owned set untouched), err naming count/limit/remedies - fails loud at deploy time (attended) instead of leaving a silent partial monitoring gap discovered mid-incident. fill (explicit opt-in) = keep owned, add lowest-id additions into remaining slots (deterministic across a fleet), warn naming every skipped VLAN. VLAN_COUNT_WARN renamed VLAN_WARN (default 32) for the VLAN_* family symmetry; nothing released so rename is free. Pure helpers vlan_guard + limit_fill (tests 1f, 12 asserts, RED->GREEN); gate_vlan_count orchestrates; wired into do_boot (incl. relocation branch), do_rescan, and dry-run (count-gate preview line). PRD v3.2 FR-36/AC-13.
Rejected: fill as default (silent-gap risk outweighs partial-monitoring benefit; operator must opt into the tradeoff); dropping owned VLANs to satisfy the limit (removals only ever come from boot reconcile detection, never from the cap).

## 2026-07-23 - Round-3 targeted review (accept primitive + relocation + gate): mid-revert false-accept closed
Why: Third review (Fable), scoped to the round-2 fixes and VLAN_LIMIT (the code no reviewer had seen). Verdict WARNING: 0 CRITICAL, 1 HIGH, 2 MEDIUM, 3 LOW, plus explicit clean bills on the fifo lifecycle, probe pre-existence (provably cannot pre-exist, incl. the relocation old-parent case), run_detection concurrency (bare `wait` safe: apply is terminal in every mode, no other background children), gate_vlan_count plumbing (log() is stderr-only), apply_change deltas, and bash-3.2 parse. All findings fixed same day (script 1270 lines):
- HIGH mid-revert false ACCEPT: liveness alone cannot distinguish "in confirmation window" from "mid-revert". If health fails all window, try's timer fires and its revert RESTORES routing; the loop could then see consecutive PASSes while try is still alive finishing the revert, and the buffered newline lands as a false ACCEPT for a rolled-back change (then deletes + restarts + no restore_prior). Fix: confirmation-window bound - track seen_at (first apply evidence) and forbid accepting once waited - seen_at >= TRY_TIMEOUT - 2*POLL_INTERVAL (try's revert timer starts at or before seen_at); past the bound = FAIL path.
- MEDIUM probe semantics: the probe netdev proves the apply BEGAN, not completed (netdev creation is async after the networkd restart; uplink routing effects can land seconds later). Fix: APPLY_FALLBACK_SETTLE floor is now unconditional before health sampling (was probe-bypassed); PRD/design wording corrected ("has begun").
- MEDIUM silent zero-monitoring under RESET_ON_BOOT=false relocation: pinned to a dead parent, everything logged at info only. Fix: warning-level breadcrumb whenever owned parent shows no tags while another NIC carries tags, regardless of RESET_ON_BOOT.
- LOWs: dead no-change check inside the relocation branch removed (removals=owned is non-empty by construction); ref's carrier state added to the relocation notice (idle-vs-absent forensics; reviewer confirmed the ambiguity is an irreducible residual of passive detection, bounded by two-pass + independent-selection evidence and post-ACCEPT-only teardown); removals-only changes now pass the VLAN_LIMIT gate even while over limit (the cap gates growth; blocking a shrink would freeze an over-limit box over-limit) - FR-36 doc note added.
- Also fixed (self-caught during the edit): relocation target was computed before gate_vlan_count could shrink additions in fill mode; target now assigned post-gate.
Hardware checklist gained L3-20 (mid-revert accept guard) and L3-21 (reverted-addition ghost netdev: revert does not delete the created netdev, so the id may classify "managed elsewhere" until reboot - reviewer's out-of-scope observation, recorded as a probe).
47/47 asserts green; gate smoke (shrink-through, refuse, fill lowest-N) verified.

## 2026-07-23 - IR-1 resolved: keep the netplan minimum pinned at 0.106
Why: User asked whether a netplan minimum should be required and approved keeping it. 0.106 is a validated-floor pin, not a feature-floor pin - it is the version on the hardware-validated Ubuntu 22.04.5 box (context/decisions.md 2026-07-14 baseline). The true technical floor is lower (`netplan try` is ~0.95-era; the four DHCP-override isolation keys use-routes/use-dns/use-ntp/use-domains stabilized ~0.98-0.100), but nothing below 0.106 has been run through the apply/revert path, the whole fleet is Ubuntu 22.04+, and the safety posture ("never strand a headless box", "exercise before claiming") says do not advertise support for a version never executed. check_preconditions (dynavlan:396-405) both refuses below MIN_NETPLAN and capability-probes `netplan try --help`; the probe confirms the subcommand exists, the version pin is what actually backstops headless fifo-revert semantics, so keeping the pin conservative matters more than the probe. IR-1 closed in open_questions.md.
Rejected: relaxing the pin toward the technical floor (would require running the Layer 3 hardware checklist on a box at the proposed lower version first - an honest lower bound is confirmed-on-hardware, not a changelog number; not worth it for an all-22.04 fleet).

## 2026-07-23 - README generalized for initial release; non-Meraki logged as future work
Why: User asked to reposition the README away from Protectli/Domotz-only toward generalized dynamic VLAN discovery with service-restart integration, for the v0.1.0 pre-release. README rewritten: dynavlan is now the landing subject (was "domotz-deployment"), framed as vendor/hardware-agnostic VLAN discovery; Domotz/Protectli demoted to "the reference deployment". Added a pre-release/initial-testing status banner (user-requested) and an explicit "do not run on a box you cannot reach at the console" caveat. The RESTART_SNAPS/RESTART_SERVICES seam is described as "service-restart integration": after a successful VLAN change dynavlan restarts nominated snaps/systemd units so any agent that enumerates interfaces only at startup (LibreNMS/Zabbix/netdisco/Domotz/custom collector) re-reads and monitors the new subnets. Non-Meraki switch detection logged as a post-v0.1 open question (LLDP VLAN-table advertisement varies by vendor; sniff-primary proven only on Meraki; silent low-traffic VLANs invisible to sniff regardless of vendor).
Rejected: n/a (docs-only; no behavior change).

## 2026-07-23 - Round-4 scoped review (Fable cold read): restore_prior loudness + dry-run lock; no fourth full review
Why: After switching the session to Fable, a cold read of the full 1270-line script found no CRITICAL/HIGH and confirmed the round-3 fixes in place. Decision (user-approved): do NOT run a fourth full review over unchanged code (three rounds already turned the high-severity space over; the dominant residual risk - netplan/networkd behavior on the box - is only findable in Layer 3). Instead a targeted pass on the three cold-read observations:
- MEDIUM restore_prior unchecked cp (fixed): the disk-convergence copy-back on the failure/revert paths was silenced and unchecked; a failure (disk full at the worst moment) silently left disk holding the reverted config while live state was the prior one, corrupting the next run's owned-set premise. Now checked; on failure logs err naming the surviving LAST_BACKUP path for manual recovery (being loud is the only safe remedy at that point - live state is already settled by netplan's revert).
- LOW dry-run vs flock (fixed): --dry-run ran outside the FR-30 lock, so a timer rescan could start applying mid-preview (stale/misleading preview; harmless to the network). Now: flock -n - if free, hold for the preview (a mid-preview rescan skips per its normal contention path and retries next cycle); if held, warn "preview may reflect mid-change state" and proceed read-only rather than block the operator.
- Portability spot-check (no change): stat -c %a/%u (+ -f fallback for macOS test sourcing), sync FILE (coreutils >= 8.24; 22.04 ships 8.32), sort -V, flock, mktemp -u, timeout, and the 0$mode & 022 octal arithmetic all verified fine for Ubuntu 22.04.
PRD deliberately unchanged: neither fix alters an FR (FR-30 serializes runs; dry-run remains read-only per FR-34; FR-19's convergence intent now just fails loud). Design §6/§7 and tests Layer-2 updated; CHANGELOG Unreleased bullets added. Script 1283 lines; bash -n clean; 47/47 asserts green. Next: Step 5 hardware checklist.
Rejected: fourth full review (diminishing returns on unchanged bytes); blocking dry-run on the lock (operator tool must not hang behind a 60s+ sniff run); exclusive-only lock semantics for dry-run (warn-and-continue preserves read-only availability).

## 2026-07-23 - VLAN_ROUTES routed mode added (FR-37), user-requested, pre-hardware
Why: User asked for a switch to bring discovered VLANs up WITH routes at per-VLAN priorities plus a starting-priority setting. The reference deployment (dev/IMPLEMENTATION-SPEC.md) does exactly this by hand (7 VLANs, metrics 10..700), so the use case is proven. Config: VLAN_ROUTES (default false - isolation default unchanged), VLAN_ROUTE_METRIC_START (default 100, >=1), VLAN_ROUTE_METRIC_MODE (default discovery). discovery mode (user's chosen default): existing VLANs keep their persisted metric VERBATIM forever; new discoveries take max(START-1, highest existing)+1 ascending - later discoveries never renumber established priorities. Assignments persist in 90-dynavlan.yaml itself (backend_owned_metrics reads them back; the owned file doubles as the state store, no new state file). id mode (option): stateless metric = START + id, fleet-identical. DNS/NTP/domains stay declined in both modes (routes only, never the resolver). RECOVERABILITY guard: apply_change computes the map at its single choke point and metric_conflict REFUSES up front (before any disk change) if any assigned metric <= the pre-apply uplink default's metric - that VLAN's default would win the routing table and guarantee an FR-18 health-FAIL revert loop; refusing loudly beats revert-looping mysteriously. Empty-snapshot stance: with routes on and no pre-apply default, a VLAN default may become the uplink; documented as intended failover (PRD FR-37) rather than prevented. TDD per the iron law: tests 1g written RED first (17 asserts: assign discovery/id, map_filter, metric_conflict), then helpers GREEN; wiring verified off-hardware (config accept/reject matrix, routed generate round-trip incl. owned_metrics re-read, missing-metric refusal, isolated-mode byte-identical). 64/64 green. PRD bumped v3.3 (FR-37, AC-14); design §5/§11, tests 1g + L3-22/23 + AC map, conf template, README, CHANGELOG updated. Sequencing: user chose to land this BEFORE the Step 5 hardware pass so one trip covers both modes.
Rejected: positional consecutive metrics (100,101 by sorted position - a new low-id VLAN would renumber every other VLAN's metric each reconcile, churning config and shifting priorities fleet-wide); making routed mode the default (isolation is the safety posture; routes are the opt-in); a separate state file for assignments (the owned YAML already round-trips through every reconcile).

## 2026-07-23 - deployment-guide.md rewritten: dynavlan deployment only; base Domotz runbook removed
Why: User directed that docs/deployment-guide.md hold only dynavlan deployment steps, not the Domotz-on-Ubuntu provisioning runbook (company-internal content in a now-public repo). Rewritten as the dynavlan deployment guide: prerequisites, install (scp + install.sh, changes nothing on the network), configuration highlights, first attended run at the console (dry-run -> --boot -> verify -> timer), ongoing-operation command table, files-on-the-box map, removal procedure. The removed runbook (Ubuntu install, SSH hardening, UFW, Domotz snap + plugs, iPerf3, static 01-netcfg.yaml example with the 7-VLAN metric ladder) lives in the internal runbook and in this repo's git history (last at commit bed1d35); dev/IMPLEMENTATION-SPEC.md retains the architectural description. Cross-references updated: CLAUDE.md (project blurb + doc-roles row), README (doc table; dropped the "repo also holds the runbook" sentence), PRD §2 (now cites dev/IMPLEMENTATION-SPEC.md for the static approach), IMPLEMENTATION-SPEC (runbook pointer + dropped "full content in deployment-guide"), SKELETON header. Dated historical entries in context/ left as-is (they record the past).
Rejected: keeping both in one file (two audiences, one public); deleting IMPLEMENTATION-SPEC's base-deployment description (still the architectural context dynavlan builds on - flagged separately for a possible trim).

## 2026-07-23 - Project fully pivoted to dynavlan: all Domotz-deployment content removed; Domotz stays only as the restart example
Why: User directive - the repo began as Domotz-deployment documentation and became the dynavlan project; the only Domotz content that belongs is how to use dynavlan to restart the Domotz snap. Swept every tracked file. Removed: dev/IMPLEMENTATION-SPEC.md (was entirely the base-deployment spec; survives in git history). Generalized: RESTART_SNAPS default now EMPTY in script/conf/PRD FR-28 (behavior change - existing deployments must set it explicitly; the Domotz snap name stays as the documented example in conf/PRD/README); PRD §1 summary, §2 problem, §6 delta table, AC-2 rewritten vendor-neutral; CLAUDE.md project blurb likewise; SKELETON header pointer dropped; context/index.md refreshed (was stale: "before building dynavlan", v3.2, Step-1 next action). Kept deliberately: Protectli/igb/Meraki references in hardware-validation contexts (PRD §10, CLAUDE.md Non-Obvious Behaviors + Workflow, tests L3 preamble) - that is dynavlan's own validation record and test bed, not deployment steps; README service-restart section's Domotz example (exactly the permitted usage); dated historical entries in context/ (they record the past).
Rejected: keeping RESTART_SNAPS defaulted to the Domotz snap (a generic tool should not silently restart a vendor agent; example over default); scrubbing Protectli/Meraki from validation records (would falsify the hardware provenance the safety case rests on).

## 2026-07-23 - CLAUDE.md optimized: 139 -> 116 lines, one home per instruction, live state exiled
Why: User asked whether CLAUDE.md had grown too long and what to externalize. Changes: (1) Non-Obvious Behaviors section (7 hardware-validated bullets) collapsed to a pointer - all seven now live in dev/SKELETON.md (populated today), two homes = drift risk; (2) live status statements ("code-complete, not yet exercised", "next action Step 5") removed - status lives ONLY in context/todo.md (this class of line went stale twice today); (3) Start Here no longer re-enumerates the read-order (context/index.md owns it); (4) doc-roles table de-versioned (v3.2/1a-1f had already gone stale); (5) stale template frontmatter dropped; (6) Working Rules folded into the Change Checklist; (7) deduped 3x "exercise before claiming" to Testing Plan, 2x "blast radius" to Development Workflow, 2x "hardware-agnostic" to Design Principles; (8) dropped "prefer immutable operations / small functions" clause (template boilerplate contradicting the deliberate mutable-globals + long-expanded-functions design; line-cap override already recorded) and the em-dash rule (already in the global ~/Claude/CLAUDE.md loaded every session). Nothing lost: every dropped line exists verbatim elsewhere or contradicted the repo's design.
Rejected: keeping the Non-Obvious bullets inline for auto-load visibility (Start Here + Development Workflow already mandate reading SKELETON before touching those paths).

## 2026-07-25 - FR-0 netplan version probe fixed: `netplan --version` does not exist on the target fleet
Why: First hardware run of `sudo dynavlan --dry-run` refused with `netplan >= 0.106 required (found unknown)`. Root cause: `netplan_version` probed `netplan --version`, a flag netplan only grew in 1.0. The validated baseline (Ubuntu 22.04, netplan.io 0.107.1-3ubuntu0.22.04.4) rejects it and prints usage to stderr, so the regex matched nothing, the empty result was treated as "too old", and dynavlan refused on 100% of target boxes. The version pin was never at fault: 0.107.1 clears the 0.106 floor comfortably, and `version_ge` passed its new tests unmodified, confirming the comparator was correct all along. This was a probe that had never been exercised: IR-1 (2026-07-23) reasoned carefully about WHICH version to pin while the code could not read ANY version, which is exactly the failure mode the repo's "never claim it works until it has run on the box" rule exists to catch.
Fix: `parse_version` extracted as a pure, unit-testable helper (first `N.N[.N]`, empty when none, handles dpkg epoch `1:` and revision `-3ubuntu0.22.04.4` forms). `netplan_version` now tries sources in order, first hit wins: `netplan --version` (netplan >= 1.0) then `dpkg-query -W netplan.io` (the 0.10x fleet). Only stdout is parsed (`2>/dev/null`, was `2>&1`): the 0.10x usage dump names things like `ethernets.eth0.dhcp4=true` and scraping a help message for a version number is a false positive waiting to happen. The two failures are now logged distinctly - "cannot determine netplan version" vs "netplan >= X required (found Y)" - because an operator cannot act on the old message. Both still refuse, per FR-0's fail-toward-no-change posture.
Checked `netplan info` first as a packaging-independent source (it would suit the hardware-agnostic principle better than dpkg): on 0.107.1 it reports feature flags only, no version, so it is not usable. Accepted consequence, documented in README/deployment guide: a netplan 0.10x box with no dpkg cannot be version-checked and is refused.
TDD per the iron law: tests 1h written RED first (6 parse_version cases + 4 version_ge cases, plus `ver_ge`/`ver_lt` assert helpers), then GREEN; 74/74 asserts pass. Collector verified off-hardware against simulated device/1.x/no-source environments. PRD FR-0 amended (it had cited `netplan --version` as the example probe, propagating the wrong assumption), plus CODEMAP, SKELETON, tests 1h + L3-13a, README, deployment guide, CHANGELOG. No version bump: 0.1.0 is unreleased.
Rejected: `netplan info` as a source (no version field on 0.10x); parsing stderr to catch versions printed in usage text (false-positive prone, and there is no version there anyway); lowering MIN_NETPLAN (the floor was never the problem); treating an undeterminable version as a pass (silently proceeding without the version backstop on a headless box inverts the FR-0 safety posture).

## 2026-07-25 - install.sh hardened for the upgrade path (timer quiesce + FR-30 lock)
Why: User asked what happens running ./install.sh over an older install. It works as an upgrade (script and units overwritten, config preserved, deps skipped, enable is a no-op), but the safety reasoning at install.sh:87-90 ("Install must never trigger a surprise network change", hence enable WITHOUT --now) only holds for a FIRST install. On an upgrade the rescan timer has been firing every RESCAN_MINUTES since the last install, and install.sh took no lock, so: (a) a rescan could fire seconds after the swap and run the NEW code unattended before the operator's --dry-run preview; (b) worse, `install -m 0755` could rewrite the script file under a LIVE `dynavlan --rescan`. Bash reads a script lazily by file offset, so an in-place rewrite makes the running invocation execute garbage or stop mid-logic - demonstrated locally: the running script silently exited 0 without executing the rest of its body. If that landed between `netplan try` and its confirmation, the run dies, the kernel releases the fd-held flock, and netplan auto-reverts. Safe-by-design rather than stranding, but it puts the L3-21 reverted-add ghost-netdev path in play for no reason.
Fix: install.sh stops dynavlan.timer for the duration if it was active, restores it on EVERY exit path via a trap (never leave a box with rescans silently off because the install aborted), and takes the same FR-30 lock (/run/dynavlan.lock) with `flock -w 300` before touching the script, refusing loudly rather than swapping under a live run. 300s because a rescan legitimately runs for minutes (sniff + try window + lease settle). Lock released after the enables. Also fixed a pre-existing lie in the closing message ("The rescan timer is already active"), which was false on a fresh install where the timer is deliberately enabled-but-not-started; the message is now conditional on whether this was an upgrade, and on an upgrade it warns that the new code will rescan within RESCAN_MINUTES.
Verified on the real box (macOS has no flock, so the local test was inconclusive): all three paths exercised in /tmp as a non-root user, touching nothing - fresh install proceeds without stopping anything; upgrade stops and restores the timer; lock held by a simulated live run refuses, restores the timer, exits 1, and leaves the script unswapped.
Rejected: leaving the timer stopped after an upgrade and telling the operator to restart it (the user's ask was that a bare `sudo ./install.sh` be safe without remembering a dance; a silently-stopped rescan timer is its own footgun); a non-blocking `flock -n` (an upgrade should wait for a normal in-flight rescan, not fail on it).

## 2026-07-25 - Docs recommend `sudo bash install.sh`, not `sudo ./install.sh`
Why: On the test box the operator hit "Permission denied" and had to `chmod +x install.sh`. install.sh is correctly mode 100755 in git; the exec bit was lost in TRANSFER - the box's whole repo dir arrived 664/775 with group-write under umask 0002 (the signature of a GitHub ZIP download, rsync without -p, or a hop through a filesystem with no permission bits). `dynavlan` itself arrived non-executable too, which is harmless: install.sh places it with `install -m 0755`, setting the destination mode regardless of the source. Only install.sh needs its own bit because it is invoked directly. `sudo bash install.sh` is immune to the whole class. README + deployment guide updated with the reason inline.
Rejected: telling operators to chmod (treats the symptom every time, on every box); shipping a wrapper or Makefile (a second artifact to solve a documentation problem).

## 2026-07-25 - Hardware finding: orphaned VLAN links mask what a rebooted box would do
Why: On the box, `--dry-run` proposed additions [18 100] out of detected [1 18 21 22 100 101]. The exclusion of 21/22/101 turned out NOT to come from the netplan config: the user had removed the `vlans:` block from /etc/netplan/01-netcfg.yaml during the session (original saved as 01-netcfg.yaml.bak), so `netplan get network.vlans` returns null and /run/systemd/network/ contains only the two ethernet .network files. The vlan1/21/22/101 links are still UP in the kernel as ORPHANS from an earlier apply - netplan does not tear down virtual devices it has stopped managing. `backend_list_managed_vlans` unions netplan config with `ip -d link show type vlan`, so the live orphans alone suppressed adoption. VLAN 1 was excluded separately by VLAN_MIN=2.
Consequence to carry forward: dynavlan's managed-VLAN exclusion is deliberately conservative (do not fight over a live link), but it means a dry-run on a box with orphaned VLAN links does NOT predict post-reboot behavior. Here, once the orphans are gone, additions become [18 21 22 100 101] and dynavlan would adopt VLANs the operator had hand-tuned with route metrics 100/200/300 - and would bring them up route-ISOLATED, since VLAN_ROUTES defaults to false. Flagged to the user as a decision to make before --boot, not silently handled.
Rejected: treating the null `netplan get network.vlans` as a dynavlan bug (it is correct - the config genuinely no longer declares them); auto-cleaning orphaned links (dynavlan owns one namespace and must not delete links another config created).

## 2026-07-25 - FR-7a: LLDP must never contribute the native/pvid VLAN
Why: First real hardware apply provisioned `enp1s0.100`, which came up and never leased ("VLAN 100 acquired no lease within 30s"). Verified cause: `lldpctl -f keyvalue enp1s0` returns exactly `vlan.vlan-id=100` + `vlan.pvid=yes`. LLDP was the ONLY source contributing 100 (the sniff never saw a tagged VID-100 frame, because on the native VLAN there are none), and `detect_lldp` grepped every `vlan-id=` line while discarding the adjacent `pvid` flag. The native VLAN is untagged on the wire, so an interface carrying its tag can never receive a frame: it comes up dead and re-warns on every rescan forever.
Correction on the record: the first explanation offered to the user - that the DHCP server was refusing a second lease for the same MAC on 192.168.100.0/24 - was a guess and was wrong. The interface is not a duplicate, it is unreachable. Worth remembering as a pattern: "plausible mechanism" was asserted before the evidence (`lldpctl` output) was actually read.
Fix: new pure helper `lldp_tagged_vlans` parses the keyvalue blocks statefully (lldpctl emits no per-VLAN index, so `vlan-id` and its `pvid` are correlated by ADJACENCY alone) and drops `pvid=yes`. A block with no `pvid` key is treated as tagged - absence of the flag is not evidence of native. `detect_lldp` delegates to it. TDD RED->GREEN, 6 asserts in section 1i, suite 74 -> 80.
Deliberately scoped to the LLDP source only: `detect_iface` unions sniff + LLDP, and if a switch carries the native VLAN ID tagged as well, the sniff sees those frames independently and the VLAN stays a legitimate candidate. The rule is "the PVID is not evidence of a tagged VLAN", not "the PVID is banned".
Rejected: `VLAN_IGNORE="100"` (the user's instinct, correctly, was that this should be automatic). It is a per-site manual step for something discoverable from the wire, would have to be rediscovered and re-entered at every site, and directly contradicts the stated principle "no hardcoded interface names, VLAN IDs, native VLAN, or base filenames". The config edit was attempted on the box and blocked by the permission classifier; nothing on the box was changed.
Process note: FR-7 already stated "Meraki MS130 advertised only pvid 100" and dev/SKELETON.md:32 repeated it. The observation was recorded twice and acted on zero times - it was treated as a reason to default DETECT_METHOD=both, never as a reason to filter. Documented facts are not implemented behavior; worth re-scanning the docs for other noted-but-unhandled observations.

## 2026-07-25 - FR-21a: the rescan timer had never fired, on any install
Why: Verifying the FR-7a deploy, `systemctl list-timers dynavlan.timer` showed `n/a` in every column (NEXT/LEFT/LAST/PASSED). `systemctl show` confirmed it: `TimersMonotonic={ OnUnitActiveUSec=5min ; next_elapse=0 }`, `NextElapseUSecMonotonic=infinity`, `LastTriggerUSec` empty, `Result=success`, unit active. OnBootSec=2min is declared in dynavlan.timer but was ABSENT from the merged runtime state.
Root cause: `do_reconfigure` rendered a drop-in opening with `OnUnitActiveSec=` to override the base interval. In systemd an empty assignment to ANY On*Sec= resets the ENTIRE monotonic timer list, not just the option assigned, so that line also deleted OnBootSec. The timer was left anchored solely on OnUnitActiveSec, which elapses relative to the PREVIOUS activation of dynavlan-rescan.service; that service had never activated, so there was no anchor and no next elapse. The in-code comment ("OnUnitActiveSec accumulates across drop-ins, so clear the base value first") shows the accumulation was understood and the reset's blast radius was not.
Severity: this is the failure mode the PRD calls CRITICAL - "silently no-op the whole tool". FR-21 periodic discovery has never worked on any install. Every VLAN observed appearing on the box came from the boot reconcile. Nothing logged an error; the unit reported active and successful throughout. The only tell was `n/a` in list-timers, which nobody looks at on a box that appears healthy.
Fix: extracted pure `render_timer_dropin` (testable; do_reconfigure just writes it) which restates EVERY monotonic trigger after the reset. First fire is OnActiveSec (relative to timer activation) rather than only OnBootSec (kernel boot), because install.sh now stops and restarts the timer on upgrade, by which point a boot-relative deadline is already in the past - OnActiveSec covers boot and upgrade alike. Base unit carries OnBootSec + OnActiveSec + OnUnitActiveSec and a WARNING comment naming the cross-file invariant, since adding a trigger there without adding it to render_timer_dropin silently drops it again.
Tests: 1j asserts the rendered text as an exact whole-file match, which pins ordering too (the reset must precede the re-assignments or it wipes them), plus a standalone regression guard that a first-fire trigger is always present. Hardware row L3-24 added, because the unit test can only prove the text - only the box proves the timer elapses, and "active + Result=success" proves nothing since the dead timer had both.
Process note: this was found only because a deploy was verified against live systemd state instead of being assumed to work. Two of the three defects this session (FR-0, FR-21a) were invisible to unit tests and code review by construction: they live in the interface between the script and the platform. Worth extending that suspicion to the other systemd/netplan touchpoints - the journald drop-in, the boot service ordering, and `netplan try`'s capability probe.

## 2026-07-25 - FR-5a: detection counted our own egress, making removal impossible
Why: After deploying the FR-7a fix, `detect_lldp enp1s0` correctly returned empty, yet the rescan still logged `detected=[1 18 21 22 100 101]`. So 100 was now coming from the SNIFF, not LLDP. A 15s capture showed nothing (wrong conclusion available here: "it's gone"); a 75s capture split by direction showed exactly one VID-100 frame, OUTBOUND, from our own MAC 00:00:5e:00:53:be - a DHCP Request from 0.0.0.0 - and zero inbound. The dead enp1s0.100's DHCP client retries forever, tcpdump captures egress as well as ingress, and dynavlan counted its own transmission as proof the VLAN is live.
Consequence, larger than the VLAN that exposed it: EVERY owned VLAN transmits tagged DHCP on the parent (retries if unleased, renewals if leased), so every owned VLAN is permanently self-detected. FR-23 two-pass removal can therefore never fire - not for a VLAN created in error, and not for one the switch genuinely stopped trunking. The dead interface was self-perpetuating across reboots: the config recreates it, it transmits, the sniff sees it, removal is suppressed. Deleting the link by hand does not help either, since the owned YAML recreates it on the next apply.
Fix: `tcpdump -Q in` (inbound only) in detect_sniff. Only frames the switch actually delivers are evidence of what the trunk carries. Added a precondition probe rather than letting it degrade silently: `tcpdump -Q in -d vlan` compiles the filter and exits without opening the device (non-mutating, works unprivileged, verified on the box) - if -Q is unsupported the run refuses, because the silent alternative is bidirectional capture and permanently suppressed removals. Consistent with FR-0's no-silent-fallback stance on `netplan try`.
Rejected: filtering `not ether src <our mac>` (works, but -Q in is the direct expression of the intent and does not need the MAC threaded into the filter); leaving it unprobed (this session produced two defects that were invisible precisely because a platform seam failed quietly).
Not unit-testable: this is a one-flag change inside an impure shell-out. Hardware row L3-25 carries it instead - confirm an owned VLAN whose only traffic is ours drops out of `detected`, and that a de-trunked VLAN is actually removed by --boot.
Process note: the first hypothesis (egress self-detection) was right, but the first TEST of it was too short (15s) and appeared to disprove it. Nearly recorded "not our traffic" on that. A negative result from a window shorter than the phenomenon's period is not evidence of absence - the sniff window is 60s, so the probe had to be at least that long.

## 2026-07-25 - VLAN_MIN default 2 -> 1: VLAN 1 is a real VLAN, not switch management
Why: On the validated trunk, `--dry-run` reported `detected=[1 18 21 22 101]` but VLAN 1 never became a candidate. `compute_candidates` intersects against `[VLAN_MIN,VLAN_MAX]` and VLAN_MIN defaulted to 2, so VLAN 1 was filtered out before any other rule saw it. On this network VLAN 1 is tagged on the trunk and carries a live DHCP-served subnet (192.168.255.0/24) that the operator wants monitored, while the actual management/native VLAN is 100 (untagged, carrying the box's own uplink address).
Root cause is an assumption, not a bug: the default encoded "VLAN 1 == switch management VLAN == noise" as a wire fact. It is a convention, and this trunk violates it in both directions - VLAN 1 is production, and management is elsewhere. Excluding it by default silently drops a subnet from monitoring, with nothing logged: a filtered-by-range ID is not reported as an exclusion (unlike managed-elsewhere IDs, which get a per-VLAN warning via log_exclusions), so the gap is invisible unless someone diffs `detected` against `owned` by eye.
Fix: VLAN_MIN default 1. VLAN_MAX stays 1000. The range reverts to what it should always have been - a coarse numeric bound - and VLAN_IGNORE remains the mechanism for skipping specific IDs, which is where a site-specific "VLAN 1 is management here" belongs.
Supersedes the 2026-xx entry above noting "VLAN_MIN default is 2 (PRD authoritative); the earlier design-locked line saying VLAN_MIN=1 is SUPERSEDED". The original design said 1, PRD v3 changed it to 2 on the management-VLAN assumption, and hardware has now settled it back to 1. Note the decision log recorded the flip to 2 without any evidence for it, which is how it survived four review rounds.
Rejected: keeping 2 and documenting the caveat louder (the operator still has to know to look, and the tool's whole premise is that nobody logs into the box); making the exclusion loud instead of removing it (a per-run warning for every in-range-filtered ID is noise on a correctly-configured box, and would have to fire on every rescan forever).
Consistent with "discover, don't assume": VLAN 1's role is a property of the site, discoverable only from the wire or declared by the operator, never assumable from the ID.

## 2026-07-25 - FR-14a: VLAN isolation covered DHCPv4 only; IPv6 RA walked in unfiltered
Why: `ip address` on the box showed `enp1s0.22` holding a global SLAAC address (`fdf5:3b99:bcd9:4208:...`, `dynamic mngtmpaddr noprefixroute`) while every documented isolation key was in force. Reading backend_generate_config confirmed why: all four `use-*: false` keys live under `dhcp4-overrides` and constrain DHCPv4 alone. IPv6 Router Advertisement is a separate, unsolicited channel, and systemd-networkd accepts RAs by default on a non-router link, so nothing dynavlan wrote had any bearing on it.
Severity is RECOVERABILITY, not confidentiality, and that only became clear on reading FR-18. PRD line 133 states the health check "is valid ONLY because FR-14 guarantees generated VLANs install no default route" and warns that any change to the FR-14 keys must revisit FR-18. That guarantee was IPv4-only in fact while stated unconditionally, and the detector shares the blind spot: `default_routes_tokens` runs `ip route show default` with no `-6`. So a monitored VLAN could install an IPv6 default route, and both the guarantee meant to prevent it and the check meant to catch it would miss it - the precise uplink-hijack FR-18 exists to prevent, in the one address family nobody looked at.
Fix: `accept-ra: false` on every generated VLAN, emitted as a sibling of `dhcp4:`. Kills SLAAC addressing, RA default routes, and RDNSS in one key. Emitted in BOTH modes: under `VLAN_ROUTES=true`, FR-37's conflict guard reasons only about metrics dynavlan assigns, and an RA route arrives with a metric it never chose, so routed mode would bypass the guard entirely rather than merely widen it. FR-18's dependency clause now names both channels and states explicitly that the v4-only sample is sufficient ONLY while accept-ra is false.
Rejected: a `VLAN_ACCEPT_RA` config key (speculative - wanting IPv6 monitoring implies more design than one toggle, and every other isolation key is hardcoded); `link-local: []` (would also strip the fe80 addresses every VLAN carries, much larger blast radius for no benefit here); extending the health check to `ip -6` in this change (once VLANs cannot install an IPv6 default we no longer CAUSE the condition, and the accept-ra fix should not be entangled with the rollback primitive - deferred to todo, and the PRD now requires it in the same change as any future IPv6 route acceptance).
Not unit-testable: one line inside impure backend_generate_config. Hardware row L3-26 covers it, including whether the pre-existing SLAAC address is actually withdrawn on apply - unknown, and the same class as the orphaned VLAN links networkd declined to clean up. Also unverified from the dev box: that netplan 0.107 accepts `accept-ra` on a `vlans:` entry. `--dry-run` proves it for free, since backend_validate runs `netplan generate` against a throwaway tree.
Process note: found by reading a routine `ip address` paste rather than by any test, review, or checklist - the sixth defect in two days living in the script/platform seam. The pattern is now unmistakable: every one was a place where a documented guarantee was never confronted with live platform state. Worth auditing the remaining stated guarantees the same way, starting with the ones FR-18 depends on.

## 2026-07-25 - FR-38: build provenance (--version + install-time commit stamp)
Why: A fix was deployed and its symptom persisted, and I concluded from that alone that the fix did not work. It had never been installed - the deploy landed in the two-minute gap between the VLAN_MIN edit and the accept-ra edit, so the box ran a tree containing one and not the other. Nothing on the box could distinguish "the fix is present and ineffective" from "the fix is absent", and the wrong branch was taken. The correction came only from noticing that the generated netplan file, which dynavlan writes itself, lacked the key entirely.
Root cause is missing identity, not a bad inference: `ver` was 0.1.0 across every build that day, so every candidate build self-reported identically. `--status` printed it, but needs root and runs a ~90s detection pass, and could not have separated those builds anyway. The only usable signal was accidental - `range=2-1000` versus `range=1-1000` in a log line - which works solely because that particular change happened to alter a logged value. A change with no logged side effect (accept-ra is exactly that) leaves no trace at all.
Fix: `--version`/`-V` printing `dynavlan <ver> (build <id>)`, dispatched BEFORE load_config and before the root check - an invalid config or an unprivileged shell are precisely when the question is asked, so gating the answer behind either would defeat it. `install.sh` stamps the checkout's short commit, `-dirty` when the tree has uncommitted changes, `unknown` for a non-git install. The identity also goes on every `run start:` line and into the generated netplan header, so provenance is recoverable retroactively from the journal with no extra command and no foresight at deploy time.
The `-dirty` marker is the load-bearing part, not the hash. Today's deploy would have stamped `18a6ea2-dirty`, which says "this corresponds to no commit" - the exact fact whose absence caused the misdiagnosis. Deploying a working tree mid-edit is not an edge case; it is the normal way a half-finished change reaches a box during a hardware session.
Stamping is awk, not `sed "0,/^build=/s|...|"`. The 0,/re/ address is a GNU extension that BSD sed rejects, so the sed version could not be exercised on the macOS dev box at all - an install step testable only in production, which is the same class of problem as the defects this session produced. Caught by writing the test first and watching it not substitute. install.sh additionally greps for the stamped line and runs `bash -n` before installing, and refuses on either failure: a botched substitution must never become the script that runs as root on a headless box.
Also fixed while in install.sh: the new temp-file cleanup was written as a second `trap ... EXIT`, which in bash REPLACES the existing handler - it would have silently dropped yesterday's timer-restore trap, leaving rescans off after any install that aborted. Consolidated into one `cleanup` handler installed once, with a comment naming the constraint. And `[ "$build_id" = "unknown" ] && say ...` was a set -e landmine: when the test is false the list returns non-zero and the script exits, which would have aborted every successful install. Both were mine, both found by re-reading the diff rather than by running it.
Rejected: bumping `ver` per build (semver is a release identity, not a build identity, and this needs to work between releases); an install timestamp instead of a commit (says when, not what); runtime `git rev-parse` from /usr/local/sbin (there is no checkout there).
Process note: three defects in this one small change, all self-inflicted, all caught before deploy by testing the stamping end-to-end on the dev box and re-reading the diff. That is the loop that has been missing - the session's other defects were all found by the box because nothing here exercised them first.

## 2026-07-25 - Review round 5 (first review since hardware; three parallel reviewers)
Why: eight changes had landed since the last review (round 4, 2026-07-23, pre-hardware) - FR-0, the install.sh live-run hardening, FR-7a, FR-21a, FR-5a, FR-14a, the VLAN_MIN default, FR-38 - all written reactively during a live debugging session, several touching detection and the generated config. CLAUDE.md requires reviewing every modified unit before a minor release, and 0.1.0 is queued. 268 changed script lines. Reviewers: code review of the dynavlan diff, bash-correctness review of install.sh, security review of both.
Verdict: no CRITICAL, no HIGH from the reviewers; one MEDIUM I raised to HIGH on evidence, plus four LOW. Nothing that can strand a box.

RAISED TO HIGH AND FIXED - `lldp_tagged_vlans` key-shape matching. The awk matched `/\.vlan-id=/` and `/\.pvid=yes/`, i.e. ANY key with those suffixes, not the VLAN TLV's own `.vlan.` subtree. The reviewer reported it as MEDIUM (native VLAN misclassified as tagged when a foreign `.vlan-id=` key lands between a vlan-id and its pvid) and was explicit about not being able to confirm lldpd ever emits such a key. Writing the RED test surfaced a second, worse effect they had not found: the foreign key's own VALUE leaks out as a VLAN candidate (`30 200` where only `30` is real), so dynavlan would build an interface for a VLAN that is not on the trunk at all. Fabricating VLANs from unrelated TLV data is a correctness failure beyond misclassification, hence HIGH.
The unverifiability is the reason to fix rather than a reason to wait: the parse depended on an assumption about a key set nobody here has documented, controlled by the switch. That is the same shape as every defect this session - a guarantee resting on an unconfirmed platform behavior. Anchoring on `.vlan.` removes the dependency entirely, costs nothing, and all five original fixtures still pass. Two regression cases added to 1i.

Fixed (LOW): `usage()` did not advertise the working `-V` alias. install.sh claimed in past tense that the rescan timer "has been restarted" while the restart actually happens in the EXIT trap fired after that text prints - harmless in practice, but this project has already been bitten by documentation asserting more than the code does, so the tense is now accurate. install.sh dirtiness now uses `git status --porcelain` rather than `git diff --quiet HEAD`, which also counts UNTRACKED files: deliberately over-marking, since a false "dirty" costs a second look while a false "clean" is a build identity that lies. And a comment now names the charset dependency that makes the unescaped `build_id` interpolation safe (git hex SHA + literals this script writes), so nobody reuses the pattern for a branch name or tag, which can carry quotes and backslashes into a root-run script.

Confirmations worth keeping, both stronger than what was documented: FR-14a's `accept-ra: false` closes ALL RA-delivered channels together (SLAAC, RDNSS, DNSSL, Route Information Options), not just the default route, since one `IPv6AcceptRA` knob gates them, and `dhcp6` is never set so DHCPv6 was already off - no residual IPv6 acceptance channel remains on VLAN interfaces. And `compute_candidates` evaluates ids in bash arithmetic, where `((x))` on a non-numeric string EXECUTES embedded command substitution; every id source (LLDP parse, sniff, `netplan get`, `ip -d link`) enforces digit-only before storing, so a hostile switch cannot reach it - load-bearing rather than incidental, and worth not breaking.
Process note: the reviewer's repro was correct but incomplete, and the gap only closed because the fix was TDD'd - writing the failing test first exposed the value-leak the report missed. Reproduce a finding before accepting its severity, and write the test before the fix even when the diagnosis looks obvious.

## 2026-07-25 - FR-37 routed mode only ever worked greenfield (metric assignment set)
Why: while adversarially reviewing the FR-39 `--reapply` proposal, a reviewer traced the metric path for the zero-additions case and noticed the same flaw applies to the partial-additions case that exists today. Reproduced against committed code before accepting it:
  kept_map=[]  additions=[22]  ->  ROUTE_METRICS=[22:100]
  dynavlan[err]: internal: no route metric assigned for VLAN 1 (map [22:100]); refusing to generate
Root cause: `apply_change` fed `assign_route_metrics` the ADDITIONS as the set needing a new metric. The correct set is "target ids that do not already have a persisted metric". Those coincide only while every owned VLAN already has one - i.e. only if routed mode was enabled before the box ever adopted a VLAN. An operator turning `VLAN_ROUTES=true` on an existing box leaves every owned id metric-less, `backend_owned_metrics` returns empty (an isolated stanza has no `route-metric:` line to read back), and generation refuses.
Consequence: routed mode was usable ONLY greenfield. Enabling it on a deployed box froze ALL applies - including new VLAN adoption - permanently, until the setting was reverted. Loud but misleading: the `internal:` prefix reads as a code defect rather than "this state is unreachable from here", which would have sent diagnosis in the wrong direction. HIGH / CONTINUITY; no stranding, since the refusal precedes `netplan try` and live state is untouched.
Fix: new pure `plan_route_metrics OWNED_MAP TARGET ADDITIONS START MODE` computing `needs = target - ids(kept)` and delegating to `assign_route_metrics`. Deliberately ONE function rather than a documented call sequence: `--reapply` (and any later drift check) must compute the map identically to the apply path, and two call sites recomposing `map_filter`/`assign_route_metrics` themselves is precisely how they silently diverge - which the reviewer flagged as a structural prerequisite, not a style preference. New `map_ids` helper. 8 asserts in 1g, RED before GREEN. Verified idempotent: re-planning from the file just written, with zero churn, returns the identical map and never renumbers a live VLAN's priority.
Note this was reachable today, with no relation to FR-39. It survived four review rounds and a hardware session because nobody had enabled routed mode on a box that already owned VLANs - the feature was specced, implemented, unit-tested against its own happy path, and never exercised along its actual upgrade path. Same shape as the session's other defects: the tested path and the real path were different paths.
Process note: the reviewer reported it as a consequence of the FR-39 proposal. Reproducing it first showed it was a live defect independent of the proposal, which changed both its severity and its sequencing (it must land before `--reapply`, since a reapply runs with zero additions by definition and would hit it immediately). Reproduce before accepting a finding's framing, not just its severity.

## 2026-07-25 - FR-39 `--reapply`: config regeneration was gated on the VLAN set, so generated-config fixes never deployed
Why: FR-14a (`accept-ra: false`) was written, reviewed, committed, installed on the box - and provably inert. `dynavlan:1283` skips regeneration when additions and removals are both empty, and the box's owned set already equalled detected, so no run ever rewrote the file. Confirmed live: `grep -c accept-ra /etc/netplan/90-dynavlan.yaml` -> 0, and `ip -6 route show default` still showed an RA-installed default via a monitored VLAN. dynavlan self-heals its VLAN SET but never its own CONFIG FORMAT.
Scope, decided by the operator: there is ONE box. Automatic drift detection on boot/rescan is NOT built. The first draft of this design spent its effort on whether an upgrade should trigger unattended applies fleet-wide, which is a risk that does not exist yet; per "smallest thing that works", only the manual mode is built. The reviewed design for the automatic version is recorded in todo.md so resuming is cheap.
What ships: `--reapply` regenerates the owned set with the running build, compares, and applies only on a real difference.
Design points that are load-bearing rather than incidental, each from a review finding reproduced before acceptance:
  - NO detection. Pinned to `owned_parent`, refuses without one. With detection, trunk selection landing on a different NIC would silently convert a reapply into a trunk RELOCATION - every VLAN renamed onto another parent - with none of FR-23's two-pass evidence and no cleanup of the abandoned links.
  - Lease-wait the FULL owned set, not additions (empty here). Both reviewers found this independently: rewriting every stanza can make networkd reconfigure existing links, and restarting an interface-enumerating agent mid-re-DHCP would DROP kept subnets from inventory - a monitoring-loss regression caused by the fix itself.
  - Count gate BYPASSED, not merely expected to pass. Zero additions is not growth, and in `fill` mode the gate would TRIM the owned set, turning a reapply into a silent removal.
  - Blocking lock with a timeout. `--boot`/`--rescan` are scheduled and simply run again next cycle, so "skipped, rc 0" is harmless there; for an operator command it would read as success while nothing happened.
  - Comparison ignores exactly line 1, POSITIONALLY. FR-38 put a per-build id in that header this afternoon, so a whole-file comparison would report a difference after every rebuild and apply unconditionally - a no-change apply on the one operation that can strand a box, i.e. two features each fine alone and hazardous together. Positional rather than a pattern match on header text: a pattern match would silently stop matching if the header format changed, and that failure mode is an apply loop rather than an error.
Rejected: md5 of the file (detects hand-edits only - it cannot see that what we WOULD write differs, which is the entire problem); hashing generation INPUTS (cheap and catches both upgrade and config drift, but a forgotten input is a SILENT false negative - the exact failure mode behind this session's defects, whereas comparing the artifact cannot forget an input); a header-vs-content hybrid (the header check existed only to make the frequent unattended rescan path cheap, and with no rescan check there is no cost to avoid).
Stated limitation, deliberately in the PRD rather than only here: this compares FILES, so it proves file equality and never behavioral equality. If netplan accepts a key and silently fails to emit it, a reapply flips the comparison to identical and manufactures positive evidence that the fix landed when it did not. FR-14a was caught by `ip -6 route show default`, never by reading YAML. L3-28's runtime assertions are the real proof.
Also fixed in passing: `do_dryrun` still hand-composed the old metric sequence after the FR-37 fix moved it into `plan_route_metrics`, so a preview would have shown a different map than the apply it previews. Exactly the divergence the new function's own comment warns about, missed in the commit that wrote that comment - two call sites is already one too many.

## 2026-07-25 - FR-39 + FR-14a verified on hardware; two open unknowns answered
`--reapply` (build e225a1c) ran on the box and did exactly what it was designed to do. Evidence, not inference:
- Drift detected on a box where owned == detected, i.e. the case where every other mode logs "no change" and rewrites nothing. This is the gap FR-39 exists to close, closed.
- `netplan try ACCEPTED`, then ALL FIVE owned VLANs were lease-waited before the snap restart. That is the full-owned-set lease wait working: pre-fix, `wait_leases "$additions"` would have waited on an empty set and restarted the agent immediately, while kept VLANs were potentially mid-re-DHCP. Both reviewers predicted this independently and the log confirms the corrected behavior.
- Second `--reapply`: "on-disk config already matches what this build generates; no change, nothing applied", start and end inside the SAME SECOND (04:52:04 -> 04:52:04) versus ~95s for any mode that detects. Idempotence and the no-detection design both hold.

ANSWERED - does networkd withdraw an already-installed SLAAC address when IPv6AcceptRA flips to no? YES. This was explicitly recorded as unknown (the orphaned VLAN links precedent suggested networkd might leave it, needing a manual `ip -6 addr flush`). It did not: the pre-existing global address on enp1s0.22 was withdrawn by the apply, no manual step required. RA-derived addresses are NOT the same class as orphaned links.
ANSWERED - does netplan 0.107 honor `accept-ra` on a `vlans:` entry, or merely parse it? It HONORS it: `IPv6AcceptRA=no` appears five times in the generated `/run/systemd/network/*.network`, and `LinkLocalAddressing=ipv6` is retained so fe80 survives. Verified earlier against a throwaway `--root-dir` tree and now in the live runtime files.

Process note: two self-inflicted false alarms in the verification itself, both caught by checking rather than reporting. (1) I read a 121s wall time as the reapply's duration when it was my own polling loop - a `--collect` unit reports SubState `dead`, not empty, so the break never fired; the real duration came from journal timestamps. (2) I reported the Domotz snap as `inactive` from a unit name I GUESSED; the real unit is `...domotzpro-agent-deamon.service` (misspelled inside the snap) and it was active and running the whole time. Both are the same error as the FR-14a misdiagnosis earlier today: asserting from a proxy instead of reading the artifact. The verification commands themselves need the same skepticism as the code.

## 2026-07-25 - Review round 6 (the FR-37 metric fix + FR-39 --reapply)
Scope: `73dba5f..HEAD`, the two commits that landed after round 5 and were therefore unreviewed, both touching the apply path (`apply_change` gained a parameter; a whole new mode that applies). Reviewed BEFORE the console revert drill on purpose - the drill deliberately stresses this code, so findings belong ahead of it.
Verdict: no CRITICAL, no HIGH. Two LOW. The reviewer verified by execution rather than reading: mocked `backend_generate_config` into failure and into succeed-then-vanish, ran the pure functions directly, and re-derived `plan_route_metrics`' completeness (`kept-ids u needs = target` by construction, so `backend_generate_config`'s per-id metric lookup can never be starved and hard-refuse).
Confirmed clean and worth keeping recorded: every `do_reapply` exit path restores the global `NETPLAN_FILE` and removes the tmpdir (a leak there would make a later apply write the real config into /run); `${4-$2}` is the correct form for the new `lease_set` parameter, distinguishing unset from explicitly-empty, and all three existing call sites pass exactly 3 args so none is affected; `do_reapply`'s pre-swap `plan_route_metrics` call and `apply_change`'s internal recomputation build byte-identical arguments from the same file under the same lock, so the candidate diffed is what the apply writes; `--reapply`'s blocking flock cannot deadlock against boot/rescan's non-blocking one; 110/110 under the dev box's actual bash 3.2.57.
DISAGREED with the reviewer on one LOW and fixed it anyway. They found (independently of my own trace) that a failed read-back leaves `$generated` empty, which compares DIFFERENT against a non-empty live file and applies - and concluded "fails safe, no fix needed", correctly noting the apply is regenerated from scratch, health-gated, and idempotent. True: it cannot strand or corrupt. But it is an apply with no reason behind it, on the one operation that can strand a box, and FR-39 exists precisely to honor "no change = no apply". The precedent is FR-5a's `-Q in` probe, where we chose a loud refusal over silent degradation for a case just as unlikely. Three lines to turn a baffling silent misread into a stated refusal. Verified the guard actually fires by mocking generate-then-delete: err, rc 1, no apply.
Accepted the other LOW without a code change: command substitution strips trailing newlines, so bodies differing ONLY in trailing blank lines compare SAME. Unreachable as used (both sides come from `backend_generate_config`, which never emits them), so a comment naming the limit is the right size of response - the hazard is a future reuse against a file written by something else.

## 2026-07-27 - Revert path validated on hardware (drill driven over serial); routed mode too
The FR-18 rollback path - the central "never strand the box" guarantee, and the ONE thing that had never fired on hardware - was exercised end to end on the box and passed the full contract:
  post-apply health check FAILED; netplan try reverted; uplink preserved, no deletes, no restarts   (rc=1)
Every clause confirmed independently after the revert: exactly one default route (enp1s0 metric 10, box reachable throughout), 7/7 owned VLAN interfaces still present (no deletes), Domotz agent still active (no restarts), and restore_prior had converged the on-disk file back to the pre-try config (7 route-metric lines). Also validated the FR-18 confirmation-window bound: the accept refused via "confirmation window elapsed without sustained health" rather than timing out silently.

How the fault was staged, and the first attempt's instructive failure:
- The box's own guards make a health-FAIL unstageable in software (metric_conflict refuses any VLAN metric <= uplink). So the fault is an externally-injected competing lower-metric default route added DURING the netplan try window, after the snapshot, making the lowest-metric default egress a non-snapshot iface.
- Attempt 1 attached that route to enp1s0.100 - a VLAN interface dynavlan RECONFIGURES during the apply. netplan's reconfiguration of that iface FLUSHED the injected route before health sampled, so health passed and the routed config was (correctly, from its POV) ACCEPTED. The fault was hung on the very thing being changed.
- Attempt 2 attached it to enp2s0 - the dead second NIC dynavlan never touches. It persisted through the whole window; being link-down the kernel never actually forwarded through it, so the health check read it as lowest-metric-wrong-iface and failed while the box's real connectivity was untouched. Reliable trigger, zero disruption. LESSON for testing this system: inject faults on interfaces dynavlan does NOT manage, or they get flushed by the apply.

Incidental but real: attempt 1 validated ROUTED MODE (FR-37) on hardware for the first time - VLAN_ROUTES=true reapply gave all 7 owned VLANs ascending default-route metrics 100-106, uplink stayed lowest at metric 10, health passed, accepted. That exercised the plan_route_metrics fix (2026-07-25) on the exact isolated->routed migration path that was broken, on real hardware. The box was returned to isolated afterward (clean reapply, drift routed->isolated).

Driving mechanism: the whole drill ran over the USB-serial root shell (tmux "Serial Console" on the Mac, send-keys + capture-pane), with SSH used only for read-only polling. This is the console-access workflow the repo requires for apply/rollback testing - and it worked, so the rollback path is no longer un-exercised.

Box state now: on a UniFi switch (L2) with the Meraki still doing DHCP/routing; native VLAN is 101 (uplink 192.168.101.39 untagged on enp1s0); owned/monitored set is [1 18 20 21 22 100 200] (VLAN 101 dropped as the new native; VLAN 100 now a real tagged VLAN, adopted). IPv6 hole closed, isolated mode.

## 2026-07-28 - all-trunks provisioner redesign: DESIGNED + APPROVED (not yet implemented)
Decision: reposition dynavlan from single-trunk monitoring provisioner to a GENERAL provisioner that configures detected VLANs on EVERY carrier-up trunk, not one selected. Driven by the operator's product-direction call (a "dynamic VLAN detection/enabling utility" should provision all trunks, and doing so removes the unpredictable "most tags wins" selection contest entirely).
Full design: docs/superpowers/specs/2026-07-28-all-trunks-provisioner-design.md (rev 2). Brainstormed collaboratively, architect-reviewed (verdict: sound to plan after 5 gate items, all folded), and core feasibility hardware-validated before any code.
Locked decisions (authoritative in the spec, section 2): all trunks; a trunk = carrier-up NIC with >=1 tagged VLAN; overlapping VLAN ids across trunks -> configure on both (distinct interfaces, no dedupe); trunk goes dark -> PRESERVE (remove an owned VLAN only when its trunk has carrier AND non-empty detection in both passes AND the id is absent from both - zero-tags preserves); isolated + discovery-mode routed together, `id` metric mode DROPPED (VLAN_ROUTE_METRIC_MODE removed); routed metrics keyed by full interface name (iface.id), discovery-order, never renumbered; unified single netplan-try apply over the whole box; stale interfaces on a vacated trunk accepted + documented (no cleanup path); health check UNCHANGED (multi-uplink is the normal case, watching the lowest/primary uplink suffices for never-strand; only untested edge is co-equal-metric uplinks).
THE load-bearing implementation invariant (spec section 3): `iface.id` (e.g. enp1s0.100) is the universal token for EVERY VLAN set - owned, additions, removals, target, count-gate input, metric map, lease-wait, probe. Bare VLAN-id sets alias across parents the moment two trunks share an id; that is the single thing most likely to be gotten wrong.
Removes: select_trunk + hysteresis + TRUNK_IFACE-as-single-global; the AC-3 relocation branch (obsolete - no single pin to strand on); VLAN_ROUTE_METRIC_MODE and assign_route_metrics' id branch.
Architect findings folded (spec section 12): B1 iface.id universal token (was under-stated), B2 two-pass carrier authority + flap semantics, B3 corrected removal rule (zero-tags preserves, contradicted the original decision-4 wording), H1 relocation-removal not equivalent -> stale interfaces accepted+documented, H2 health check unchanged (reframed: multi-uplink normal not rare), M1 parse stanza header for owned map, M2 count-gate fill/blocking notes, plus per-interface managed-elsewhere, warn_overlap/FR-11 parent-awareness, FR-4/AC-3/FR-35 revisions, migration test.
Hardware validations banked pre-code (2026-07-28): netplan 0.107 accepts multi-parent + overlapping VLAN id (enp1s0.100 and enp2s0.100 emit distinct netdevs); per-trunk detection reads each trunk's distinct tagged set on the wire; per-trunk native handled for free (untagged native never sniffed) - proven on the live two-trunk box where 100 is tagged-on-enp1s0 / native-on-enp2s0 and 101 the reverse.
Version: recommend 0.2.0 for the behavioral boundary (0.1.0 still unreleased). Existing single-parent owned files remain readable (degenerate multi-parent).
NEXT: writing-plans skill -> implementation plan -> TDD implementation. REQUIRED deliverable: reframe README/PRD/deployment-guide to general-provisioner positioning (lands WITH the code, not before), and resolve the deferred positioning open question.

## 2026-07-29 - All-trunks provisioner implemented (ver bumped to 0.2.0)
Why: the approved design (2026-07-28 entry above) was implemented. `select_trunk` and its hysteresis contest are gone; `run_detection` now sets `DETECTED_TRUNKS` to every carrier-up iface with a non-empty tag set instead of picking one. Every set downstream of detection (candidates, additions, removals, owned, target, route-metric maps) is now keyed by `iface.id` tokens via new pure helpers (`emit_tokens`/`tok_iface`/`tok_id`/`tag_tokens`/`untag_tokens`/`tokens_for_iface`/`distinct_ifaces`), so two trunks sharing a VLAN id provision as two distinct interfaces rather than aliasing. `do_boot`/`do_rescan`/`do_dryrun` loop per-trunk candidate/removal math (bare ids, scoped to that trunk) over the union of owned-or-detected trunk ifaces, tag each trunk's result to tokens, combine into one token set, and make ONE unified `apply_change`/`netplan try` call for the whole box - never a per-trunk apply, since revert has to be atomic across every trunk's change together. `backend_owned_vlans`/`backend_owned_metrics` parse `iface.id` stanza headers across however many parents the file spans; `backend_list_managed_vlans` now takes an `iface` argument (a VLAN id managed elsewhere on one trunk must not exclude the same id on a different trunk - the collision case the token design exists for). `owned_parent` is gone; replaced by `distinct_ifaces($(backend_owned_vlans))` wherever a caller needs "which trunk(s) do we own." The AC-3 relocation branch is removed: a trunk that goes dark (no carrier, or tagless in both boot passes) simply has its owned VLANs preserved, never torn down and rebuilt on a different NIC - the branch existed only because the old model forced a single "the trunk," and a genuine cable move is now just one trunk losing tags while another (independently provisioned on its own merits) carries them. `VLAN_ROUTE_METRIC_MODE` (and its `id` mode) is removed from the config surface: metric assignment is discovery-order only, one ascending sequence shared across the whole box, since a stateless START+id scheme has no coherent meaning once VLAN ids are no longer globally unique keys - `plan_route_metrics`/`assign_route_metrics`/`map_filter`/`metric_conflict`/`map_ids` all operate on token:metric maps now.
`ver` bumped to 0.2.0 (behavioral boundary: token representation, removed config key, removed relocation semantics - all breaking relative to 0.1.0's owned-file format and config schema).
Docs (dev/CODEMAP.md, dev/SKELETON.md, dev/features/dynavlan.md, dev/features/dynavlan-tests.md, CHANGELOG.md, README.md, docs/dynavlan-PRD.md, docs/deployment-guide.md, context/todo.md, context/open_questions.md, context/index.md) updated in the same pass to match, per CLAUDE.md's "docs land WITH the code" rule. New test sections 1n (token helpers), 1o (multi-parent backend parsing), 1p (multi-trunk pipeline integration); 1e (trunk hysteresis) retired, not reused, so historical references stay unambiguous; new hardware rows L3-29..L3-32 for the multi-trunk paths, none yet run.
Positioning: resolved the deferred open question (see context/open_questions.md) - dynavlan is now framed as a general dynamic-VLAN provisioner with a monitoring origin, not a monitoring-only tool. Isolation stays the default (unchanged); routed mode is presented as a first-class supported configuration rather than a niche exception. This is a framing change only, not a default-value change.
NOT changed: the FR-18 health check (multi-uplink is normal behavior now that more than one trunk can carry a default route; watching the lowest-metric default suffices for never-strand, per the design spec's H2 finding), the apply/rollback state machine's shape (still snapshot -> generate -> validate -> apply_with_revert -> accept/fail), and backup/logging/flock mechanics.
Hardware validation of the multi-trunk paths (dual leasing, carrier-pull-preserve, routed multi-trunk, unified revert across two trunks - dynavlan-tests.md L3-29..L3-32) is explicitly NOT done yet; the box already has two live trunks wired up as the fixture for it (see context/todo.md live state).
Rejected: keeping bare VLAN ids as the key with a "first trunk wins" collision rule for overlapping ids (silently drops a real, distinct subnet on the second trunk - unacceptable for a provisioning tool); keeping a relocation branch that moves an old trunk's set onto a newly-selected one (no longer coherent once there is no single selected trunk to relocate FROM).

## 2026-07-30 - v0.2.1 released (all-trunks redesign + netplan-get fix)
Why: all-trunks provisioner (v0.2.0) and the netplan-get field-order fix (v0.2.1) are hardware-validated on the Protectli box with two live trunks. All four multi-trunk test cases passed (L3-29 dual leasing, L3-30 carrier-pull preserve, L3-31 routed multi-trunk, L3-32 unified revert). CHANGELOG reformatted into proper Keep a Changelog sections. Tagged v0.2.1, pushed, GitHub release created on pereljon/dynavlan. GitHub repo description updated from Domotz/monitoring framing to general dynamic-VLAN provisioner.
Rejected: releasing as v0.2.0 (the field-order fix warranted a patch bump before tagging).

## 2026-07-30 - One-line curl installer (get.sh)
Why: simplest zero-infrastructure install path. Downloads the latest (or specified) release tarball from GitHub and runs install.sh. Pattern: `curl -fsSL .../get.sh | sudo bash`. No apt repo needed.
Rejected: requiring users to clone the repo or manually scp artifacts (too many steps for a first install).

## 2026-07-30 - .deb packaging with GitHub Actions release workflow
Why: enables `dpkg -i` installs and is the prerequisite for a future APT repo (so `apt upgrade` picks up new versions automatically). The workflow triggers on `release: published`, builds the .deb on ubuntu-22.04, and attaches it to the release. Package uses `/lib/systemd/system/` (package-managed) and postinst handles migration from install.sh's `/etc/systemd/system/` placement.
Rejected: Launchpad PPA immediately (more infrastructure than justified with one deployed box; .deb-on-release is the building block, APT repo is the next step when user base justifies it). Full debhelper build system (overkill for a single bash script with no compilation; dpkg-deb --build is sufficient).

## 2026-07-30 - README status block trimmed
Why: the multi-line pre-release status block was replaced with a single-line version + validation stamp. The safety warning about console access was softened ("if possible, run with out-of-band fallback") since dynavlan may be preinstalled or remotely installed without console access, and "--dry-run" is the alternative. Full safety detail remains in the Safety section.
Rejected: removing the status block entirely (the version + validation stamp is useful context at the top).

## 2026-07-30 - Keep ONE owned netplan file; file-per-trunk considered and rejected
Why: Question raised while designing carrier-down VLAN removal (docs/superpowers/specs/2026-07-30-carrier-down-vlan-removal-design.md): would a file per trunk (90-dynavlan-<iface>.yaml) beat the single 90-dynavlan.yaml? Rejected. The single-file design matches four constraints that are all box-wide, not per-trunk: (1) the apply is already unified - one `netplan try` window must cover every trunk at once, and `netplan generate` merges all *.yaml regardless, so splitting shrinks no blast radius and just adds a merge step to own; (2) the file IS the state store - `backend_owned_vlans`/`backend_owned_metrics` read the owned set and routed-mode metric map back out of the one file keyed by iface.id token, so N files make the state store a set to enumerate/merge/keep consistent, with a new failure mode (half-written or orphaned file) at the read every reconcile depends on; (3) backup/rollback is the safety-critical chain - `backup_current` snapshots the file and `restore_prior` converges disk back to the one prior file, so splitting means snapshotting/restoring a SET atomically and a partial restore becomes a brand-new way to strand the box, added at exactly the place the design refuses extra moving parts; (4) one atomic mktemp+fsync+rename and one hardcoded ownership path become N non-atomic writes plus a `90-dynavlan-*.yaml` glob, softening the "exactly one hardcoded path, everything else discovered" principle. The multi-trunk aliasing case is ALREADY solved by iface.id token keying within the one file (enp1s0.100 and enp2s0.100 are distinct stanzas, no collision), so a split would solve a problem the tokens already eliminated. Interaction with carrier-down removal: `rm 90-dynavlan-<trunk>.yaml` looks tidier than dropping stanzas, but explicit `ip link delete` per interface is still required either way (netplan apply alone won't tear the netdev down) and the apply is still one `netplan try`, so the file boundary buys no safety while adding orphaned-file cleanup - ironic for a feature whose point is reducing cruft. The one real upside (provisioned trunks visible from `ls`) is already covered by `dynavlan --status` and the per-stanza iface.id headers in the file. Reaffirms the "One file owned" invariant (dev/SKELETON.md Key Invariants, CLAUDE.md Design Principles).
Rejected: file-per-trunk (90-dynavlan-<iface>.yaml) - complicates the safety-critical apply/rollback chain and the state-store read for cosmetic gains already provided by --status and stanza headers.

## 2026-07-30 - Restart-on-new-subnet implemented (FR-40, ver bumped to 0.3.0)
Why: FR-27's restart trigger only fires on a tagged-VLAN change, missing two real cases - an access port or native-only trunk that gets a DHCP lease after boot (no tagged VLAN, so dynavlan sees no change), and the monitoring agent starting before base-interface DHCP completes (FR-29 deliberately does not block boot on `network-online.target`). Full design: `docs/superpowers/specs/2026-07-30-restart-on-new-subnet-design.md`. Seven decisions locked and implemented:
1. Trigger on a gained global-scope IPv4 subnet, not carrier - carrier fires before the lease exists, so restarting on it would relaunch the agent against an address-less interface.
2. Token is `interface:subnet`, keyed on the network address + prefix via the new pure helper `ipv4_network`, never the box's own host address - a same-pool DHCP renewal or re-lease produces an identical token and does not restart.
3. Monotonic "seen this uptime" set (union of every subnet seen since boot, never just the last sample) - a link flap back onto an already-seen subnet does not re-trigger; a reboot wipes it, correctly resetting the baseline.
4. Unified rule, no boot special-case: `new = current - seen`; if non-empty, restart once and grow `seen`. On boot the file is absent so `seen` is empty and any present subnet is "new," restarting the agent once after settle regardless of boot-vs-DHCP ordering - this is the boot-race fix, falling out of the rule with no boot-specific code.
5. `apply_change` is untouched; the growth-check is an additive, deduped second trigger called from `main` after `do_boot`/`do_rescan` returns, on every boot/rescan exit path regardless of the mode's own return code (it may restart on the base uplink subnet even after a health-FAILed/reverted or `VLAN_LIMIT`-refused apply - intended, since subnet appearance is independent of VLAN provisioning). A per-run `RESTARTED_THIS_RUN` flag, set by `restart_targets` from either trigger, dedups so at most one restart happens per run.
6. State lives in `/run/dynavlan/seen` (tmpfs, wiped on reboot - the correct lifetime), never the owned netplan YAML: the YAML persists across reboots (would break the boot-race fix) and is the safety-critical file written only inside `apply_change`'s generate/validate/try/accept chain, which `seen`'s every-rescan update cadence has no business touching.
7. New config key `RESTART_ON_NEW_SUBNET`, default true - the box exists to have its monitoring agent see the network, and "on" is what activates the boot-race fix for existing installs too.
Implementation landed across 4 commits (TDD RED-first for `ipv4_network`, 7 asserts in tests 1q): `cc9fcb4` (helper), `411fffe` (config key, ver bump to 0.3.0), `72a0cc6` (`current_subnets`/`read_seen`/`write_seen`/`maybe_restart_on_new_subnet`, wired into `main`), `c0f7f36` (`--dry-run`/`--status` would-restart delta line). This documentation pass (Task 5) brought PRD (new FR-40/AC-15, v3.6), SKELETON (invariant scoped to persistent config; growth-check noted in the side-effects invariant), CODEMAP, design doc (new §11a), test plan (1q + L3-33..36 + AC-15 traceability), README, CHANGELOG (0.3.0 entry), and this file current in the same pass, per CLAUDE.md's Change Checklist. State: code-complete, pending the L3-33..36 hardware checklist before release.
Rejected: carrier-up as the trigger (fires before a lease exists; would restart against an empty enumeration); keying the token on the host address (a routine DHCP renewal would restart on every lease cycle); storing `seen` in the owned YAML (wrong lifetime - would survive reboot and defeat the boot-race fix - and routes ephemeral, frequent churn through the one file that must stay safety-critical-writes-only); a boot-specific special case for the empty-seen rule (unnecessary - the unified rule already produces the right boot behavior with no extra branch).

## 2026-08-03 - APT repository hosting: GitHub Pages + reprepro, public, manual-upgrade only (DESIGN approved)
Why: making `apt upgrade` pick up new dynavlan releases needs a signed APT repository. Chosen approach is GitHub Pages + reprepro: everything stays in the existing repo/org, no external account or recurring cost, no third party in the trust path, works on Debian/Ubuntu/any apt distro (preserves distro-agnosticism), and the signing key/trust anchor stay under the maintainer's control. Public distribution, but upgrades are manual/attended only - the repo is deliberately NOT structured for unattended-upgrades, because auto-deploying a network-touching tool to remote headless boxes is exactly the recoverability footgun the project exists to avoid (recorded as a PRD NFR in the plan). Key decisions: dedicated RSA-4096 signing key, passphraseless single Actions secret (in GitHub Actions both an armored key and any passphrase share one trust boundary, so a co-located passphrase is near-illusory defense-in-depth while adding real CI complexity - reprepro can't take a passphrase, it delegates to gpg-agent; real protections are an offline key backup + revocation cert); distro-agnostic single suite `stable main` with architectures `amd64 arm64` (the arch:all bash .deb is duplicated into each index so clients can discover it - the list controls discoverability, not package contents); published repo is DERIVED from the GitHub release assets and rebuilt fresh each release (ephemeral reprepro db, no binaries in git, no stateful db to corrupt) and deployed via actions/deploy-pages (Pages source = "GitHub Actions"); client uses a deb822 .sources with signed-by keyring; get.sh becomes apt-aware with tarball + degraded-repo fallback. FR-38 build identity is untouched (rebuild re-indexes the already-stamped .debs, never rebuilds them); no ver= bump (dynavlan script not touched). Spec (local-only, git-excluded): docs/superpowers/specs/2026-08-03-apt-repository-hosting-design.md. State: design approved, pending user's spec review then implementation plan.
Rejected: Launchpad PPA (Ubuntu-only, contradicts the distro-agnostic design principle; also needs a source-package build path different from the existing binary .deb). SaaS hosting - Cloudsmith/packagecloud (external dependency in the install/trust path for a security-sensitive infra tool, an account outside the maintainer's control, and a published trust anchor that migrates if free-tier terms change). A gh-pages branch holding the pool + reprepro db updated incrementally (simpler CI logic but accumulates binary .debs in git history and carries a stateful db that can lock/corrupt). Passphrase-protected signing key (near-illusory in CI where both secrets share a trust boundary, at the cost of gpg-preset-passphrase/keygrip plumbing and a new release-time failure mode). A dynavlan-archive-keyring package for key-rotation delivery (gold standard but a second package; deferred as a future enhancement).

## 2026-08-03 - v1.0 defined: four gates + deprecate-with-warning compatibility promise
Why: needed a concrete maturity bar for the 1.0 tag rather than tagging by feel. 1.0 is an engineering maturity bar (not a marketing/positioning tag): feature-complete VLAN lifecycle (add AND remove), hardened distribution (apt-upgradable), proven beyond the reference lab, and a stated compatibility contract. Four gates, all required before the 1.0.0 tag: (1) carrier-down VLAN removal shipped as v0.4.0 (the already-planned feature); (2) APT distribution built and the reference box actually upgrades via apt; (3) second-box / multi-site validation on >=1 additional box at a different site - the non-Meraki-switch half is ALREADY MET by the v0.3.0 UniFi-trunk validation (2026-07-30), so this gate is now purely a second independent deployment, not a second vendor; (4) the 1.0.0 release itself = config-surface freeze + a published compatibility policy in COMPATIBILITY.md + version-gate git hooks (the previously-designed-not-built FR-38 enforcement) + a full major-release review run adversarially on non-Opus models (Fable and/or a GPT model; precedent: the 2026-07-23 Fable review caught the CRITICAL fifo-ACCEPT race two Opus reviews missed). Path: v0.4.0 -> APT + validation (parallel) -> freeze + tag 1.0.0 (last by construction, since the surface can only be frozen after v0.4.0 has shaped it). COMPATIBILITY PROMISE = deprecate-with-warning (chosen over semver-strict): within 1.x, additions are always free in a minor (new keys defaulting to current behavior, new flags, new log lines); breaking changes (removing/repurposing a key, removing a flag, changing a default that alters behavior on an existing box, changing an exit code's meaning, or changing generated-YAML shape in a way that breaks the owned-set round-trip or an external consumer) ship in TWO steps - a release that warns on the old form (still working), then a LATER release that removes it, never the same release; 2.0 is reserved for changes that cannot be done that way. OUT OF SCOPE for 1.0, documented as stated limitations (not silently absent): IPv6 (IPv4-only by design; FR-14a health-check arm stays deferred); exhaustive vendor coverage (1.0 validates one non-Meraki vendor, UniFi, not a broad matrix); PER_VLAN_MAC (stays opt-in, default off); automatic config-drift detection. Refactoring: no dedicated pass - single-file by design, refactor opportunistically as gate work touches a function; the Gate-4 freeze locks the EXTERNAL surface only (keys/flags/YAML shape), never internal code layout, so internal refactors stay free after 1.0. Spec (local-only, git-excluded): docs/superpowers/specs/2026-08-03-v1.0-definition-design.md. State: definition approved; context/todo.md carries the milestone; COMPATIBILITY.md + the IPv4-only limitation statement land at Gate 4.
Rejected: semver-strict compatibility (no config break without a 2.0 - too rigid for a surface still finding its shape during 1.x; the user chose the softer two-step promise). Big-bang 1.0 (hold all four gates on one branch, tag 1.0.0 in one shot) - gives up per-milestone hardware validation and contradicts the "never strand, release each validated milestone" cadence, which the deprecate-with-warning promise itself depends on (incremental releases are where a deprecation warning gets a chance to ship before the removal). Gating 1.0 on IPv6 (larger scope, longer runway; deferred to a clearly-scoped post-1.0 workstream). Requiring a second switch VENDOR for gate 3 (already met by UniFi; the residual multi-vendor matrix is post-1.0).

## 2026-08-03 - Carrier-down VLAN removal implemented (FR-41, ver bumped to 0.4.0); L3-30 superseded
Why: executed the already-approved design (`docs/superpowers/specs/2026-07-30-carrier-down-vlan-removal-design.md`) and plan (`docs/superpowers/plans/2026-07-30-carrier-down-vlan-removal.md`, 7 tasks) via `superpowers:executing-plans` in an isolated worktree. Before execution, a plan-review pass (walking the exact conditional logic: pass-1 carrier sample -> `need_pass2` -> debounce -> routing gate -> config gate, step by step, without skipping any branch) surfaced two small gaps, both folded into the plan before coding: (1) `do_rescan`'s run-start log line had no `remove_on_carrier_loss` field, unlike `do_boot`'s (observability parity - the journal alone should show effective config for every run); (2) `BOOT_SETTLE_SECONDS`'s `dynavlan.conf` comment described only its original boot-detection-diff use, but the feature reuses it as the carrier-down debounce interval too (both `do_boot`'s `RESET_ON_BOOT=false` self-settle and `do_rescan`'s settle). Both fixed in the plan and landed in code.
Implementation (7 commits, TDD pure-helpers-first per CLAUDE.md): `carrier_removals(owned_on_trunk, c1, c2)` (tests renumbered `1r` mid-execution - the plan's original `1q`/`1r` collided with `ipv4_network`'s section, already claimed by FR-40 after the plan was written) - full owned-on-trunk set iff BOTH carrier samples are `down`, else empty; `have_routing()` (tests `1s`) - composes existing `snapshot_default_route` + `has_carrier`, true iff a healthy default route exists on a carrier-up iface; new config key `REMOVE_ON_CARRIER_LOSS` (default `true`), independent of `RESET_ON_BOOT`; `do_boot`'s zero-detection guard split so a hard `run_detection` failure still aborts but an empty-but-successful detection falls through to carrier-down pruning (carrier is a separate signal from the sniff); pass-1 carrier sampled and stashed per trunk; `need_pass2` fires when `RESET_ON_BOOT=true` OR (`REMOVE_ON_CARRIER_LOSS=true` AND some owned trunk already down at pass 1) - re-detection at pass 2 gated on `RESET_ON_BOOT` only, since a carrier-only resample never needs a fresh sniff; `do_rescan` gains its first-ever removal path, fast-path-preserved (the settle sleep fires only when an owned trunk is actually carrier-down, checked against every OWNED iface not just `DETECTED_TRUNKS`); `--dry-run` previews a would-be removal (single advisory sample, `have_routing`-gated, no debounce since nothing applies); `--status` reports carrier state per owned trunk. 148/148 unit tests (139 baseline + 9 new).
The routing pre-condition (`have_routing`) sits IN FRONT of the existing apply chain, never inside it: the post-apply health check remains the sole backstop that actually runs; `have_routing` only avoids attempting a removal already known to be doomed (the dead trunk may double as the box's own uplink with no redundant route).
Docs brought current in the same pass (PRD to v3.7 with new FR-41/AC-16 and FR-21/23/AC-3 narrowed - preservation now protects only carrier-up-with-no-tags, not carrier-down; SKELETON Key Invariants + reconcile-policy prose; design doc new §11b + a §7 note on the pre-condition/post-condition split; CODEMAP; README - the "removals wait for next boot" line was stale and is corrected; CHANGELOG Unreleased; test plan gained `1r`/`1s` Layer-1 sections, Layer-2 dry-run cases, and the L3-30 revision below).
**L3-30 superseded.** The v0.2.0 hardware-validated "carrier-pull preserve" result (2026-07-30) is superseded by a revised "carrier-pull prune" case - carrier-down is no longer blanket-preserved. The original result is kept in the test plan as struck-through history, not deleted (the hardware fact that it passed under the OLD behavior is still true and worth keeping). The revised case is NOT YET run on hardware; this is a release gate, not satisfied by code + unit tests alone (CLAUDE.md: cannot claim a feature works until it runs on the box).
Deferred alternative (recorded, not built): a persistent grace-timer debounce tracking first-seen-carrier-down time in `/run`, requiring continuous down-ness for ~2 rescan cycles before pruning. Would avoid remove-then-re-add churn across a brief switch reboot or cable re-patch, at the cost of an ephemeral tracker and ~10 minutes slower cleanup of a genuine unplug. Revisit only if field data shows real churn from switch maintenance.
State: code-complete in an isolated worktree (`worktree-carrier-down-vlan-removal`, not yet merged to main). Remaining release gates: minor-release code review (all modified units, per CLAUDE.md), then hardware validation on the Protectli box (console-backed).
Rejected: the grace-timer debounce as the v1 default (more moving parts, no field evidence yet that the churn it prevents is real); checking `have_routing` only as a post-apply backstop instead of also as a pre-condition (would waste a netplan-try cycle plus an agent restart on a removal already known to be doomed); re-detecting at pass 2 unconditionally when carrier-down removal alone triggers `need_pass2` (a full ~SNIFF_SECONDS sniff would be pure boot latency for a decision that never reads detection).

## 2026-08-03 - Carrier-down VLAN removal (v0.4.0) merged to main; CLAUDE.md workflow codified
Why: closing out the carrier-down removal (FR-41) implementation session. The worktree's 7 commits merged to `main` cleanly (fast-forward), 148/148 unit tests re-verified on `main` post-merge, pushed to `origin/main`; the worktree and feature branch were then removed (all commits preserved on `main`, so nothing was lost) since the fast-forward made them redundant. Remaining gates before v0.4.0's own release are unchanged from the prior entry: minor-release code review (not yet done), then console-backed hardware validation on the Protectli box.
Separately, the session surfaced (via a live walkthrough of the do_boot/do_rescan carrier logic, and a cross-project comparison against claude-mux's CLAUDE.md) several gaps in dynavlan's own development-workflow conventions, now codified in CLAUDE.md:
- **Worktree Policy** (new section): behavior changes to `dynavlan` get an isolated worktree/branch, reported (not asked) when created since it's local and reversible; docs-only/trivial fixes stay on `main`; code review happens in the same worktree, not a separate one; a major-release review (e.g. 1.0) gets its own dedicated worktree since it spans the accumulated surface; solo-maintainer default is merge-locally over a PR.
- **Workflow Pipeline** (new section): a single canonical 12-step sequence (read state -> scope -> worktree -> code -> review -> update docs -> test -> commit -> merge -> push -> release -> session hygiene) tying together sections that were previously scattered with no ordering, mirroring a pattern already present in claude-mux's CLAUDE.md.
- **Tag-time re-verification**: immediately before `git tag`, re-run `bash tests/unit.sh` and confirm `dynavlan --version` reports the expected version - never tag off a state that hasn't been freshly verified. Mirrors claude-mux's "`make check` must pass clean immediately before `git tag`."
- **Release gate**: only tag a release if the deliverable script, its installer, or its packaged config/service templates changed since the last release; docs-only changes don't need one. Worded generically (by function, not filenames) on the user's explicit request, so the identical sentence applies to both dynavlan and claude-mux without per-project translation.
- **Change Checklist granularity**: two new "when adding X, update Y" sub-rules (config key; CLI mode/flag), mirroring claude-mux's existing pattern.
The cross-project comparison also produced `/Users/jonathan/Claude/development/CLAUDE.md` (new, coding-projects-only scope, NOT the fully-general `~/Claude/CLAUDE.md` which the user explicitly said not to touch): it records two reusable findings - (1) superpowers skills fire automatically via `using-superpowers`, so naming one in a project's CLAUDE.md only earns its place when there's a project-specific override/exemption/mapping layered on top, never as bare decoration; (2) `superpowers:code-reviewer` does not exist as a standalone name under either review mechanism (`code-review` built-in skill, or the `superpowers:requesting-code-review` subagent-dispatch pattern) - found stale in claude-mux's CLAUDE.md and fixed there same day.
claude-mux's CLAUDE.md received the mirrored treatment in the same session: added its own Worktree Policy + a "Set up a worktree"/"Merge" step in its existing Workflow Pipeline (renumbered 13 -> 15 steps); replaced its filename-specific release-gate wording with the same generic sentence used in dynavlan; trimmed its long inline "Non-Obvious Behaviors" prose into `dev/SKELETON.md` Key Invariants (mirroring dynavlan's own 2026-07-23 refactor) leaving a one-paragraph pointer; dropped a "No em dashes" line redundant with the global `~/Claude/CLAUDE.md`.
Rejected: extracting the shared workflow conventions up into `~/Claude/CLAUDE.md` (explicitly declined by the user - that file is general-purpose across coding and non-coding projects and should not carry per-repo-category workflow specifics); naming a specific review tool in dynavlan's "Code Review Before Release" (the project-specific value is the patch/minor/major scoping rule, not which mechanism executes it - reviews already trigger automatically); `/code-review ultra` as the designated 1.0 major-release review mechanism (user will decide that approach independently when the time comes).

## 2026-08-03 - v0.4.0 minor-release code review

**`run_detection` keeps its single rc; callers branch on `DETECTED_TRUNKS`.** Both first-round reviewers proposed a distinct rc for "hard probe failure" vs "detected nothing". Rejected on verification: nothing in `run_detection` can produce a hard failure, so the branch would have no producer and the empty-vs-failed distinction the docs asserted would stay fictional. Do not reintroduce it without first giving `run_detection` a real failure signal.

**`have_routing` requires a live physical NIC, not a physical egress device.** The obvious fix - whitelist the default route's egress iface against `discover_phys_ifaces` - is wrong twice: that helper filters on `[ -e "$p/device" ]` and so excludes VLAN netdevs, and it would disable FR-41 outright on a box that legitimately routes over a VPN. Requiring one physical NIC with carrier catches the all-dark case (AC-4) without either failure.

**The boot debounce interval is documented, not equalised.** At `RESET_ON_BOOT=true` the pass-2 re-detect sits between the two carrier samples, making the real interval `BOOT_SETTLE + CARRIER_WAIT + SNIFF` (~110s) versus 20s on rescan. Sampling carrier before the re-detect would leave the carrier and detection signals reflecting different instants; the longer window is preserve-biased, so it is stated in the config comment, FR-41 and AC-16 instead. AC-16's hardware flap case must pin `RESET_ON_BOOT`.

**A too-short `BOOT_SETTLE_SECONDS` is clamped, never rejected.** `dynavlan.conf` is a dpkg conffile preserved across upgrades, so tightening validation would turn an already-deployed `0` into a permanently-failing unit on a headless box - safe (it changes nothing) but silent. The clamp applies to a derived `CARRIER_DEBOUNCE_SECONDS` used only for the FR-41 carrier samples, only while `REMOVE_ON_CARRIER_LOSS=true`, and never rewrites the configured value or the sniff-comparison settle.

**The `--dry-run` over-listing was fixed despite predating v0.4.0.** Out of scope as a v0.4.0 defect, but this release's `dev/features/dynavlan-tests.md` cases assert the corrected behavior, and `--dry-run` is the operator's pre-flight for the hardware validation that gates the release. Fixing the two-line condition was smaller than rewriting the doc.

**SUPERSEDED same day (2026-08-03, evening):** the Worktree Policy above became the **Branch Policy**. Isolation from `main` comes from the branch; a worktree only adds a second working directory, which this project has no standing need for. Worktrees now require explicit user authorization and one of three reasons: a concurrent Claude session on the repo, `main` needing to stay deployable, or work that may be abandoned outright. Prompted by the v0.4.0 review session, which used a worktree for a job a branch would have done and paid a concrete cost: a worktree omits untracked and `.git/info/exclude`-hidden files, so `docs/superpowers/plans/` (including the FR-41 plan the review depended on) was missing from it. The Workflow Pipeline's step 3 and the Git Approvals list were updated to match - a branch is created and reported, a worktree is asked about first.

## 2026-08-04 - APT repository hosting implemented and merged (v1.0 gate 2, no ver= bump)

Why: executed the already-approved design (`docs/superpowers/specs/2026-08-03-apt-repository-hosting-design.md`) via a plan (`docs/superpowers/plans/2026-08-04-apt-repository-hosting.md`, 6 tasks) on branch `feature/apt-repository-hosting`. Task 1 (signing key generation) and Task 6 (live-release verification) were called out in the plan as maintainer-only actions a coding agent cannot perform - the private key must never pass through an agent session, and Task 6 needs a real release to trigger CI. Both were done live in-session by the user with the assistant only providing commands to run: `gpg --batch --gen-key` with `%no-protection` (interactive `gpg --full-generate-key` couldn't get pinentry to accept an empty passphrase, so batch mode with `%no-protection` was used instead - same key properties, no pinentry involved), revocation cert (auto-generated at key creation, moved offline), private key backed up offline and loaded into the `APT_GPG_PRIVATE_KEY` Actions secret, public key exported and committed as `dynavlan-archive-keyring.gpg`, Pages source set to "GitHub Actions". Key ID `CA1F8DAB3D00EC9D`.

Implementation (4 feature commits): `apt/conf/distributions`+`apt/conf/options` (suite `stable`, component `main`, architectures `amd64 arm64`, `SignWith: CA1F8DAB3D00EC9D`); new `publish-apt` job in `.github/workflows/release.yml` (rebuild-from-assets - fetches every release `.deb` via `gh`, runs `reprepro` against a fresh ephemeral db each run, deploys `dists/`+`pool/`+keyring to GitHub Pages via `actions/deploy-pages`, nothing stored in git); `get.sh` rewritten to try apt first (`install_via_apt`: adds the deb822 source + keyring, `apt-get install dynavlan`) and fall back to the existing tarball+`install.sh` path (`install_via_tarball`) on any failure or when a specific version is requested (apt always installs latest). Docs updated in the same pass: README/deployment-guide apt install method, PRD NFR-7 (manual/attended upgrades only, no unattended-upgrades - mechanism stays out of the PRD per the spec's PRD/docs split), CHANGELOG Unreleased, `dev/features/dynavlan-tests.md` new Layer 4 (infra verification, not unit-testable). No `ver=` bump - the `dynavlan` script itself is untouched by this feature.

**Code review (medium effort, 8 finder angles + 1-vote verify) found 6 surviving findings (4 CONFIRMED, 2 PLAUSIBLE, 2 REFUTED), all fixed in 2 follow-up commits before merge:**
- CONFIRMED, dormant/landmine: the `publish-apt` job's `for deb in debs/*.deb` loop processed `.deb`s in bash's lexical glob order, not version order. Not live at v0.4.0 (no double-digit version segment exists yet), but the first release with one (e.g. `v0.10.0` alongside `v0.9.x`) would sort the newer file first, and `reprepro` refuses to register an older version after a newer one in the same ephemeral-db run - since the job always rebuilds full history every time, this would break every subsequent publish until fixed. Fixed: sort with `ls -v` before the `includedeb` loop.
- CONFIRMED: `get.sh`'s apt failure paths couldn't distinguish "repo not live yet" (the one case the design wants tolerated) from "apt itself is broken" (dpkg lock, disk full, unrelated broken source) - any failure fell through to the tarball path, which has zero dpkg-awareness and would silently desync dpkg's database from disk on a box already apt-managed. Fixed: refuse with a clear error (rather than silently falling back) when dynavlan is already dpkg-installed and no specific version was requested (i.e. `install_via_apt` was genuinely attempted and failed for a reason other than a user-requested tarball pin).
- CONFIRMED: `gh release download ... || true` in the fetch loop swallowed every failure mode identically, so a transient API/network error on a release that DOES have a `.deb` would silently drop that version from the published repo with a fully green CI run. Fixed: surface a `::warning::` per failed tag instead of swallowing silently, and refuse loudly (`::error::` + exit 1) if zero `.deb` assets were fetched across all releases, instead of failing later on an unexpanded glob with a confusing "no such file" error.
- CONFIRMED, low severity: the keyring-fetch failure branch in `install_via_apt` didn't clean up the partially-written keyring file, unlike the other two failure branches in the same function (curl without `--remove-on-error` can leave a stray 0-byte/partial file). Verified the stray file is inert (no `.sources` file ever references it on this path), but fixed for consistency: `rm -f "$APT_KEYRING"` added to the first failure branch too.
- PLAUSIBLE: `get.sh`'s apt-get calls omitted `DEBIAN_FRONTEND=noninteractive`, unlike `install.sh`'s identical dependency-install call - given `get.sh` runs as `curl | sudo bash` (stdin is the exhausted pipe, not a tty), a future debconf prompt from any dependency could hang the install. No currently-known prompt exists in netplan.io/tcpdump/lldpd/systemd on stock Ubuntu 22.04, so not demonstrated live, but cheap to fix defensively - done.
- PLAUSIBLE: `publish-apt` had no `concurrency:` group. Near-simultaneous releases are unrealistic for this solo-maintainer, one-release-at-a-time project, but a manual re-run of a flaky old run racing a newer release's run is realistic, and whichever run's `deploy-pages` lands last wins regardless of completeness - a silent regression with a green run history. Fixed: `concurrency: { group: publish-apt, cancel-in-progress: false }` serializes runs rather than racing them.
- REFUTED: no signal-trap cleanup for the apt source/keyring in `install_via_apt` (unlike `install_via_tarball`'s `trap cleanup EXIT` for its temp dir) - verified this is correct-by-design, not a gap: once `apt-get install` is unpacking packages, dpkg's own `--configure -a`/`apt --fix-broken install` recovery is the right layer for an interrupted install, not a shell trap in `get.sh`; a trap that yanked the source mid-install would achieve nothing protective (apt doesn't re-read sources mid-unpack) and would actively remove the one thing (`apt --fix-broken install`) that lets the box self-heal.
- REFUTED: clobbering a hypothetical pre-existing `/etc/apt/sources.list.d/dynavlan.sources` with no backup - verified this scenario can't causally precede dynavlan's own installer ever running (the filename is specific to this project; nothing else would plausibly create it first), and unconditional overwrite of a vendor-owned sources file is standard, accepted convention (Docker, HashiCorp, etc. do the same).

Merged to `main` (fast-forward), 182/182 tests re-verified post-merge (unchanged - no `dynavlan` script edits, so no new pure-helper tests), pushed to `origin/main`. Feature branch removed (all 6 commits preserved on `main`).

State: v1.0 gate 2 (APT distribution) is code-complete but not yet closed - the gate requires the reference box to actually upgrade via apt, which needs a real release to exercise `publish-apt` end-to-end (the plan's Task 6, deliberately deferred: cutting a release is a maintainer action, not something to force just to close this gate). `context/todo.md` carries the remaining checklist (verify the published tree's `InRelease`/`Packages`, client onboarding test on the Domotz box, then update the `test-box-access` memory to `apt upgrade` and check off the todo item).

Rejected: incremental/persisted `reprepro` db across releases (the spec's explicit rebuild-from-assets tradeoff - simpler, no stateful db to corrupt, faithfully implemented as designed; the O(N)-per-release cost it accepts was flagged by the review's efficiency angle but is a known, already-priced-in tradeoff, not a new defect); distinguishing "package not found" from other `apt-get install` failures via exit-code/output parsing (the simpler dpkg-already-managed guard covers the concrete drift scenario without needing to reliably classify apt's failure modes, which vary across apt versions).

**Also noted, not acted on:** `.git/info/exclude` contains `docs/superpowers/`, a local-only exclude, so `docs/superpowers/plans/2026-07-30-carrier-down-vlan-removal.md` and `2026-07-29-all-trunks-provisioner.md` are untracked and invisible to `git status`. The specs directory and the restart-on-new-subnet plan predate the exclude and ARE tracked, so the state is inconsistent. These design documents exist only in the working directory and are not on GitHub.

## 2026-08-04 - Install docs restructured around apt as the recommended path (no capability removed)

Why: after the APT repository went live and was validated, the user asked whether apt should be the primary/preferred install method and whether other methods should be removed. `get.sh` already prefers apt in code (added earlier this session) - the ask was really about the docs not reflecting that hierarchy. Rejected removing any install path: the tarball fallback (`get.sh`'s `install_via_tarball`, and `install.sh` directly) is required for any non-Debian-family netplan/systemd-networkd box, which is a standing design principle (hardware- and vendor-agnostic); the direct `.deb` download covers operators who won't pipe `curl` into `sudo bash` or need to move a single file to an air-gapped box. Cutting either would be a real functional regression, not just a doc simplification.

Instead, restructured `README.md` and `docs/deployment-guide.md`: the one-line `get.sh` install is now explicitly labeled "Recommended," followed by the manual apt setup (for boxes already on a `.deb`), then an "Alternatives" list (pin a version, avoid curl-pipe-to-shell / offline transfer, no apt/dpkg at all) each with a one-line reason to reach for it. Same four install paths as before, now presented as a hierarchy instead of flat peer options.

## 2026-08-04 - External adversarial review (Codex) received, independently verified, logged as the gate-4 register; 1.0 freeze stays blocked

Why: an independent OpenAI Codex review of the pre-v1 surface (`internal/codex-review-v0-4.md`,
snapshot `main` @ `080abdc`, script still `ver=0.4.0`) landed 2 CRITICAL, 7 HIGH, 8 MEDIUM, 3
plausible, and 10 doc/release-contract contradictions, verdict "NOT READY for the 1.0 config-surface
freeze." This is effectively the gate-4 major-review (adversarial, other model) arriving early rather
than a surprise. Every finding was independently re-verified by reading the cited code/docs (line by
line): **20/20 code findings + 3 plausibles + 10 contradictions confirmed accurate, zero false
positives** - the review's line references and control-flow claims all hold. `bash tests/unit.sh` is
182/182 at this snapshot, but that suite is pure set arithmetic and does not touch the orchestration
where the findings live.

Decision: accept the findings as verified and log them as the fix register in
`docs/superpowers/plans/2026-08-04-v1.0-gate4-review-fixes.md` (full per-finding detail: severity +
my calibration, evidence file:line, verification verdict, reachability, fix sketch, `[STATUS]`).
Nothing is fixed yet - all OPEN, no code changed. The v1.0 gate (4) freeze/tag stays BLOCKED until
the CRITICAL + HIGH set is resolved with deterministic tests and console-backed hardware validation,
then re-reviewed.

Severity CALIBRATION where I differ from Codex (existence not disputed, only reach): C1
(delete-the-default-route-interface) is unreachable at the default `VLAN_ROUTES=false` and bites only
routed mode - I read it as HIGH-conditional, not a live default-config stranding path. H1
(detector-failure-as-absence) needs a vendor whose LLDP advertises TAGGED VLANs plus a sniff failure;
on Meraki/UniFi (sniff-primary, LLDP native-only) a sniff failure yields empty not partial - narrower
than framed. Everything else stands as written. Cheapest/highest-value first: the doc-contradiction
cluster (docs-only, lands on `main`), then M3/H7/H3; the real pre-freeze blockers are C2 (elapsed
time is not apply evidence for removal-only/`--reapply`), H2 (config can override safety constants),
and C1's routed-mode guard.

Codex also independently examined ~14 prior decisions and found them sound within scope (inbound-only
tcpdump, LLDP PVID exclusion, `run_detection` rc can't distinguish fail-from-empty so the fix is
adding validity producers not reinterpreting rc, `have_routing`'s live-physical-NIC clause, deletion
must stay after ACCEPT, fd-held flock, `get.sh` dpkg-managed guard, `publish-apt` serialization) -
recorded in the register's "do NOT re-open" section.

Rejected: (a) acting on any finding in this pass - the ask was to LOG in preparation for fixing, and
fixes are branch + TDD + hardware-validation work per CLAUDE.md, taken deliberately, not folded into
a logging step; (b) downgrading the "NOT READY" verdict - the two CRITICALs and the HIGH set are real
at the boundaries between individually-correct pieces, exactly where a pre-1.0 adversarial pass should
find things; (c) putting the 20-row register into a tracked `context/` file - it is a milestone's task
decomposition, so it belongs in a plan file per the Task Tracking Granularity rule (this record +
`context/todo.md` carry the tracked summary + pointer; the review and register are local-only in
`internal/` and `docs/superpowers/`, consistent with the existing local-only plan/spec convention).

## 2026-08-04 - Gate-4 fix Unit 2 (M3/H7/H3): Debian upgrade-state preservation and its transitional gap

Fixed three gate-4 findings on `fix/gate4-unit2-correctness` (ver 0.4.0 -> 0.4.1): M3 (`--dry-run`
now exits non-zero on validation FAIL), H7 (`build-deb.sh` refuses a version != script `ver=`, so a
tag that outran the bump fails the build instead of publishing a mislabeled package), and H3 (Debian
upgrades restore the operator's prior timer/service enabled+active state instead of unconditionally
re-enabling+starting the timer). Branch-reviewed (adversarial subagent): no CRITICAL; the one
Important was accepted and fixed before commit.

DECISION on the H3 dpkg handshake and its unavoidable transitional gap. dpkg runs the OUTGOING
package's `prerm upgrade` first, then the INCOMING `postinst configure`. So the prior enabled/active
state is recorded by the *old* package's prerm into `/run/dynavlan/upgrade-state`; the new postinst
restores it. Consequence: on the FIRST upgrade onto this fix (from a pre-H3 package whose prerm
records nothing), postinst finds no state file. Rather than defaulting all three dimensions to
"on" (which would re-arm a deliberately disabled timer - the exact footgun), postinst falls back to
LIVE `systemctl is-enabled` for the two ENABLE dimensions (a `systemctl stop` does not clear
enablement, so is-enabled still reflects the operator's choice) and defaults only the unrecoverable
`tmr_active` to 1 (preserve-running, availability-biased). Net: a disabled timer is never re-enabled
even on the transitional upgrade; only a stopped-but-still-enabled timer is (wrongly) re-started that
one time, and never again. Chose this over (a) hardcoding all defaults to 1 (re-arms disabled units)
and (b) trying to make the old package cooperate (impossible - it is already installed). Documented in
the deployment guide and CHANGELOG; the four-state upgrade matrix + the pre-fix transitional case are
L3-37, pending console-backed hardware validation before the release. State file lives in `/run`
(tmpfs, root-only), which is safe to source as root under the footgun-not-adversary threat model.

## 2026-08-04 (later) - H3/M3 hardware-validated on the Protectli box; transitional-disabled bug found + fixed

Validated M3 and H3 on the box over SSH with the operator's serial console up as fallback (H3's
maintainer scripts never invoke an apply; no reconcile fired at any point across ~20 min; the 14 live
VLANs + uplink were untouched throughout).

M3 PASS: `--dry-run` exits 0 on a valid tree and non-zero on a malformed BASE netplan file
(`validate: FAIL - a BASE netplan file is invalid`, correct base-vs-ours attribution), cleanup clean.

H3: the fixed->fixed matrix passed (enabled+active / enabled+stopped / disabled+stopped all restored
exactly via the prerm->postinst state file). The TRANSITIONAL case (pre-fix 0.4.0 -> fixed 0.4.1, no
recorded state) caught a real BUG the branch review and I both under-called: `tmr_active` defaulted to
`1` unconditionally, so a deliberately DISABLED+stopped timer was STARTED (active) on that one upgrade.
Not re-enabled (won't survive reboot), but re-armed for the uptime - a "never surprise the operator"
violation. FIX (this session, branch `fix/h3-transitional-disabled-timer`): `tmr_active="$tmr_enabled"`
- preserve-running only for a timer that was enabled; a disabled timer is never started. Re-validated
on hardware: transitional disabled -> disabled+inactive; transitional enabled+stopped -> enabled+active
(the one accepted transitional imperfection, self-corrects on reboot); fixed->fixed disabled unchanged.
No `ver=` bump (maintainer script only; rides 0.4.1). Lesson reaffirmed: hardware validation catches
what unit-green + review miss on the packaging/apply surface - exactly why the project gates on it.

SEPARATE PRE-EXISTING FINDING (logged in `context/open_questions.md`, NOT H3): a *fresh*
`dpkg -i`/`apt install` STARTS the timer via dpkg/systemd machinery (deb-systemd-helper likely), NOT
postinst - proven by a step-by-step repro where none of the postinst commands (`daemon-reload`,
`--reconfigure`, `enable`) start it, yet the full `dpkg -i` does; and by upgrades NOT starting an
enabled+stopped timer (so it is specific to first-install). This means the deployment guide's "install
changes nothing on the network / enables for next boot, never starts" does not currently hold on a
fresh install. Predates all gate-4 work; needs its own investigation + decision.
